# ForrestRun — Status Report

**Date:** 2026-03-17 (updated 2026-03-20)

---

## Summary

ForrestRun is an embeddable Python library for AI agent workflows — deterministic execution, full state tracking, failure recovery, and zero HTTP dependencies. It provides deterministic execution, full state tracking, failure recovery, and portable agent packaging.

This document captures the current state of every subsystem, what is implemented, what is scaffolding, and what is planned.

---

## 1. Architecture overview

The runtime executes YAML workflow definitions into durable run records with SQLite persistence. The execution model is step-sequential with conditional branching, retry/backoff, and deterministic resume/replay.

**Core flow:**
1. Load workflow definition (from file, registry, or agent manifest)
2. Create run record + initial state snapshot
3. Execute steps sequentially with retry/branch logic
4. Persist step records and state versions after each step
5. Mark run terminal (COMPLETED, FAILED, or COMPLETED_WITH_ERRORS)

---

## 2. Module inventory

### Core modules (`src/agent_runtime/`)

| Module | Purpose | Status |
|---|---|---|
| `__init__.py` | Public SDK surface — `run_workflow()`, `run_workflow_async()`, re-exports | Implemented |
| `core.py` | Executor engine, retry/branch/tool dispatch (Run/StepExecution/StepDefinition re-exported from `models.py`) | Implemented |
| `models.py` | Run, RunState, StepExecution, StepDefinition, StepStatus, RetryPolicy, NextRule dataclasses | Implemented |
| `builder.py` | `RuntimeBuilder` fluent API + reusable `Runtime` instance | Implemented |
| `defaults.py` | Factory helpers for tool registry, memory manager, LLM client, agent registry | Implemented |
| `events.py` | Typed lifecycle events (`RunStartEvent`, `StepStartEvent`, etc.) and `adapt_typed_callback` | Implemented |
| `_async_compat.py` | `run_coro_blocking()` — sync→async bridge that dispatches via worker thread when a loop is running | Implemented |
| `cli.py` | `ai` CLI — 17 subcommands (see §3) | Implemented |
| `config.py` | RuntimeConfig from `runtime.yaml`, CLI overrides, LLM registry wiring | Implemented |
| `errors.py` | Exception hierarchy (11 subclasses of `RuntimeErrorBase`) | Implemented |
| `state.py` | `RuntimeState` — namespaced state manager with diff/snapshot | Implemented |
| `workflow.py` | YAML workflow loader/normalizer, step contract validation | Implemented |
| `workflow_registry.py` | Version-aware workflow resolution from directory | Implemented |
| `function_resolver.py` | Resolve `module.function` references from `functions/` | Implemented |
| `resume.py` | Resume point determination and validation | Implemented |
| `replay.py` | Deterministic run replay from persisted history | Implemented |
| `debugger.py` | Live interactive debugger for runtime execution events | Implemented |
| `observability.py` | PII redaction, percentile calculation, observability utilities | Implemented |
| `schema_versioning.py` | Schema version comparison and migration utilities | Implemented |
| `logging.py` | Structured JSON logger (stderr) | Implemented |
| `utils.py` | Hashing, path resolution, safe_eval, template formatting | Implemented |

### Agent subsystem (`src/agent_runtime/agent/`)

| Module | Purpose | Status |
|---|---|---|
| `definition.py` | `AgentDefinition` datamodel loaded from YAML agent files | Implemented |
| `executor.py` | Bridges workflow execution and strategy implementations | Implemented |
| `prompts.py` | YAML-based prompt loading, versioning, and resolution by id | Implemented |
| `registry.py` | `AgentRegistry` — discover and index agents by id and version | Implemented |
| `strategies.py` | Agent reasoning strategies: single-call, ReAct, custom pipelines | Implemented |

### LLM subsystem (`src/agent_runtime/llm/`)

| Module | Purpose | Status |
|---|---|---|
| `registry.py` | `LLMProvider`, `ModelConfig`, `LLMRegistry` — multi-provider management, env-var credential resolution, YAML config loading | Implemented |
| `client.py` | `LLMClient` routing to provider adapters with logging | Implemented |
| `adapters.py` | OpenAI, Anthropic, Gemini HTTP adapters + Mock adapter (`LLMAdapter` Protocol) | Implemented |
| `types.py` | Shared response types (`LLMResponse`, `ToolCallRequest`) | Implemented |
| `streaming.py` | `StreamChunk`, `StreamingLLMResponse` types | Scaffolding (not wired end-to-end) |
| `sse.py` | `SSEStreamParser` for parsing provider SSE streams | Scaffolding (not wired end-to-end) |

### Memory subsystem (`src/agent_runtime/memory/`)

| Module | Purpose | Status |
|---|---|---|
| `base.py` | `MemoryTier` protocol, `MemoryManager` with namespaced deep-merge hydration | Implemented |
| `working.py` | `WorkingMemory` — scratch store (byte budget), sliding window entries, active task | Implemented |
| `episodic.py` | `EpisodicMemory` — SQLite-backed run summaries | Implemented |
| `semantic.py` | `SemanticMemory` — SQLite + FTS5, full-text search, tags, CRUD | Implemented |
| `procedural.py` | `ProceduralMemory` — key/value rule store with SQLite persistence | Implemented (no auto-learning yet) |

All implemented tiers hydrate under `runtime.memory.<tier>` using deep-merge. State corruption risk from the original shallow `dict.update` has been eliminated.

### Storage subsystem (`src/agent_runtime/storage/`)

| Module | Purpose | Status |
|---|---|---|
| `base.py` | Abstract `Storage` interface | Implemented |
| `sqlite.py` | `SQLiteStorage` — runs, steps, state_versions tables | Implemented |

### Tool subsystem (`src/agent_runtime/tools/`)

| Module | Purpose | Status |
|---|---|---|
| `base.py` | `Tool` protocol, `ToolResult`, `RuntimeContext` | Implemented |
| `registry.py` | `ToolRegistry` — store/lookup tool instances | Implemented |
| `discovery.py` | Auto-discover tools from `tools/` directory | Implemented |
| `echo.py` | Built-in `EchoTool` | Implemented |
| `http.py` | Built-in `HttpTool` | Implemented |
| `file.py` | Built-in `FileTool` | Implemented |
| `shell.py` | Built-in `ShellTool` | Implemented |
| `validation.py` | JSON Schema input validation for tools | Implemented |

### Visualization subsystem (`src/agent_runtime/visualization/`)

| Module | Purpose | Status |
|---|---|---|
| `run_loader.py` | Load run/step/state/workflow data for visualization | Implemented |
| `graph_builder.py` | Execution graph nodes/edges + branch reconstruction | Implemented |
| `timeline_builder.py` | State deltas + step timing timeline | Implemented |
| `ascii_renderer.py` | Terminal-friendly run visualization | Implemented |
| `html_renderer.py` | Standalone HTML report | Implemented |

Note: Visualizations now include run-level timing (start, completion, total duration),
per-step call timing (tool/LLM latency) alongside step durations, and
`ai visualize --html` auto-opens the browser by default (added 2026-03-18).

---

## 3. CLI commands

| Command | Purpose | Status |
|---|---|---|
| `ai init [--path DIR]` | Scaffold project (workflows/, functions/, tools/, agents/, runtime.yaml) | Implemented |
| `ai quickstart` | One-shot demo: scaffold + run a sample workflow | Implemented |
| `ai onboard [--path DIR]` | Guided setup wizard (init + provider config + sample) | Implemented |
| `ai config [--path DIR] [--check]` | Configure providers/keys, verify credentials | Implemented |
| `ai docs [--topic TOPIC]` | Open or print bundled documentation topics | Implemented |
| `ai run <ref> [-i KEY=VAL]` | Run workflow or agent (agent-aware resolution from agents/) | Implemented |
| `ai inspect <run_id> [--steps] [--state-history]` | Inspect run details | Implemented |
| `ai resume <run_id> [--workflow PATH]` | Resume failed run with workflow hash lock | Implemented |
| `ai replay <run_id> [--verify-state] [--until STEP] [--step-by-step]` | Deterministic replay | Implemented |
| `ai state-diff <run_id> [--step STEP]` | Deep state change diffs | Implemented |
| `ai visualize <run_id> [--ascii|--html|--timeline]` (alias: `viz`) | Run visualization | Implemented |
| `ai list [--agents-dir DIR]` | List available agents | Implemented |
| `ai runs [--limit N] [--html]` | List recent runs or render runs dashboard | Implemented |
| `ai metrics [--limit N]` | Aggregate health metrics across recent runs | Implemented |
| `ai test [workflows/agents/functions/tools]` | Run project-authored tests | Implemented |
| `ai export <path>` | Export project bundle (workflows, agents, functions, prompts) | Implemented |
| `ai import <bundle>` | Import a project bundle (path-traversal-safe) | Implemented |

---

## 4. Error hierarchy

All exceptions inherit from `RuntimeErrorBase(Exception)`:

| Exception | Trigger |
|---|---|
| `WorkflowValidationError` | Invalid workflow YAML |
| `StepExecutionError` | Step handler/tool failure |
| `ToolNotFoundError` | Tool name not in registry |
| `BranchResolutionError` | No matching branch rule and no default |
| `RunNotFoundError` | Run ID not in storage |
| `ReplayDataMissingError` | Incomplete data for replay |
| `ReplayMismatchError` | Replayed state diverges from recorded |
| `WorkflowIntegrityError` | Workflow YAML changed since original run (blocks resume) |
| `AgentValidationError` | Agent definition invalid or missing dependencies |
| `ConfigValidationError` | Runtime configuration is invalid |
| `StorageValidationError` | Storage schema/version is incompatible |

---

## 5. Key features detail

### 5.1 Workflow hash lock on resume

When a run is created, the workflow YAML hash is stored in the `Run` record. On `ai resume`, `Executor.resume()` compares the stored hash against the current workflow hash and raises `WorkflowIntegrityError` if they differ. This prevents resuming against a modified workflow.

**Files:** [core.py](../src/agent_runtime/core.py) (`Executor.resume()`), [errors.py](../src/agent_runtime/errors.py)

### 5.2 LLM calls and built-in handler

The runtime now includes an `LLMClient`, provider adapters, and a built-in `handler: llm` that turns workflow YAML directly into API calls.

**Capabilities:**
- Register/lookup providers and models
- Credential health check (`check_credentials()`)
- Load from `runtime.yaml` `llm:` section
- OpenAI, Anthropic, and Gemini HTTP adapters
- Native function calling support for all three providers
- Per-call cost estimation persisted to `steps.cost_usd`; `Run.total_cost_usd` aggregates

**Known gaps:** streaming output (scaffolding present in `llm/streaming.py` and `llm/sse.py`, deferred — not wired end-to-end); Responses API support.

**Files:** [llm/registry.py](../src/agent_runtime/llm/registry.py), [llm/client.py](../src/agent_runtime/llm/client.py), [llm/adapters.py](../src/agent_runtime/llm/adapters.py)

### 5.3 Project export / import

Portable project bundles via `ai export` and `ai import` in CLI. Exports include runtime.yaml, workflows/, agents/, functions/, and prompts/. `.env` is excluded. Import includes path traversal protection.

**Files:** [cli.py](../src/agent_runtime/cli.py)

### 5.4 State model

Namespaced state (`inputs`, `steps`, `runtime`) prevents cross-step key collisions and preserves ownership. `RuntimeState` manages reads, writes, snapshots, and diffs.

### 5.5 Retry and backoff

Per-step retry policy: configurable `attempts`, `backoff` (fixed/exponential), `initial_delay`. Retry state (`attempt_count`, `last_error`) is captured in `StepExecution`.

### 5.6 Conditional branching

`next` rules with `when` expressions evaluated top-to-bottom. Expression evaluation is constrained via AST validation (only `state` and `len` allowed). Default fallback supported.

### 5.7 Step contracts

Declared `inputs` (reads) and `outputs` (writes) per step. Workflow-load-time validation catches future-read and output collision errors. Runtime enforcement checks output keys against declared contract.

### 5.8 Deterministic replay

`ai replay` reproduces a historical run by injecting recorded state transitions without calling handlers/tools. Optional `--verify-state` checks reconstructed state matches recorded `state_before`.

### 5.9 Visualization

Three modes: ASCII (terminal), HTML (browser), and timeline (text). Powered by graph/timeline builders that reconstruct execution flow and state deltas from persisted data.

---

## 6. Test coverage

55 test files, 797 tests passing as of 2026-05-09. Coverage areas:

| Area | Representative test files |
|---|---|
| Core executor & runtime | `test_runtime.py`, `test_executor_e2e.py`, `test_idempotency.py`, `test_run_accessors.py` |
| Branching & resume | `test_branching.py`, `test_branch_coverage.py`, `test_branch_resume.py`, `test_resume.py` |
| Retry, replay, state | `test_retry_policy.py`, `test_replay.py`, `test_state_manager.py`, `test_state_diff.py`, `test_step_contracts.py` |
| Storage | `test_storage_roundtrip.py`, `test_transaction_safety.py` |
| Workflow + schema | `test_workflow_versioning.py`, `test_workflow_lock.py`, `test_workflow_schema_stability.py`, `test_schema_versioning.py` |
| Agents & strategies | `test_agent_definition.py`, `test_strategy_internals.py`, `test_pipeline_and_functions.py`, `test_essay_writer_wiring.py` |
| LLM adapters & client | `test_openai_adapter.py`, `test_anthropic_adapter.py`, `test_gemini_adapter.py`, `test_mock_adapter.py`, `test_llm_client_limits.py`, `test_llm_registry.py`, `test_model_regression.py` |
| Streaming scaffolding | `test_streaming_types.py`, `test_sse_parser.py`, `test_adapter_streaming.py` |
| Memory tiers | `test_working_memory.py`, `test_episodic_memory.py`, `test_semantic_memory.py`, `test_procedural_memory.py`, `test_memory_injection.py` |
| Tools | `test_builtin_tools.py`, `test_tool_discovery.py` |
| SDK surface | `test_builder.py`, `test_defaults.py`, `test_events.py`, `test_import_alias.py`, `test_async_compat.py` |
| Cost & observability | `test_cost_persistence.py`, `test_cost_and_output_schema.py`, `test_observability_supportability.py`, `test_config_latency_golden.py` |
| Visualization & debugger | `test_visualization.py`, `test_debugger.py` |
| Safety & config | `test_safe_eval.py`, `test_config.py`, `test_cli.py` |
| Reference workflows | `test_reference_workflows.py` |

---

## 7. Sample workflows

Located in `examples/reference_project/workflows/samples/`:

| File | Demonstrates |
|---|---|
| `01_linear_issue_summary.yaml` | Linear step execution |
| `02_retry_and_backoff.yaml` | Retry policy with exponential backoff |
| `03_branching_triage.yaml` | Conditional branching by severity |
| `04_fail_and_resume.yaml` | Deliberate failure for resume testing |
| `05_llm_call.yaml` | LLM provider call via built-in handler |
| `06_gemini_call.yaml` | Gemini-specific LLM call |
| `07_agent_and_function.yaml` | Agent and function step types |
| `versioning/code_review_agent_v1.yaml` | Workflow versioning (v1) |
| `versioning/code_review_agent_v2.yaml` | Workflow versioning (v2) |

---

## 8. Configuration

`runtime.yaml` controls runtime defaults: SQLite path, workflow directories, logging, and LLM provider configuration. Providers are registered under the `llm:` section and API keys are resolved from environment variables at runtime.
