[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "forrestrun"
version = "0.2.0"
description = "Embeddable workflow engine for AI agents. Deterministic, resumable, zero dependencies."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Yash Agrawal", email = "yash.agr1510@gmail.com"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
keywords = ["ai", "agents", "workflow", "llm", "orchestration", "deterministic", "replay", "resume"]
dependencies = [
    "PyYAML>=6.0",
    "typing-extensions>=4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "ruff>=0.4",
]

[project.scripts]
ai = "agent_runtime.cli:main"

[project.urls]
Homepage = "https://github.com/rogue-socket/agentic-runtime"
Repository = "https://github.com/rogue-socket/agentic-runtime"
Issues = "https://github.com/rogue-socket/agentic-runtime/issues"

[tool.setuptools.packages.find]
where = ["src"]
include = ["agent_runtime*", "forrestrun*"]

[tool.setuptools.package-data]
"agent_runtime.visualization" = ["templates/*.html"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 120
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501"]
