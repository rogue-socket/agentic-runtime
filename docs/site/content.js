window.DOCS = {"about/README.md": "**About The Codebase**\n\nThis folder explains how the runtime is structured and how it behaves under the hood.\n\n- `docs/about/architecture.md` \u2014 the execution model, data model, and extension points\n- `docs/about/status_2026-03-17.md` \u2014 implementation status snapshot\n- `docs/about/gaps_2026-03-17.md` \u2014 known gaps and planned work\n- `docs/about/git_runtime_date.md` \u2014 source control date reference\n\n**Where The Code Lives**\n\n- `src/agent_runtime/cli.py` \u2014 CLI entrypoints and orchestration\n- `src/agent_runtime/core.py` \u2014 executor and step execution logic\n- `src/agent_runtime/workflow.py` \u2014 workflow parsing and validation\n- `src/agent_runtime/storage/sqlite.py` \u2014 persistence\n- `src/agent_runtime/tools/` \u2014 built-in tool interfaces and discovery\n- `src/agent_runtime/llm/` \u2014 provider registry and adapters\n\nIf you are new to the internals, start with `docs/about/architecture.md` and then skim the execution walkthrough in `docs/guide/execution-walkthrough.md`.\n", "about/architecture.md": "<!--\nFile: docs/about/architecture.md\nPurpose: Defines the runtime's architectural model and execution contracts.\nDescription: Documents entities, control flow, state model, persistence, replay, and extension points.\nDependencies: Mirrors behavior implemented in src/agent_runtime.\nInputs/Outputs: Input for developers; output is shared architecture understanding.\nSide Effects: None.\n-->\n\n# Architecture\n\nThis document describes the runtime as an execution system: data model, control flow, persistence, determinism guarantees, and extension points.\n\n## 1. Runtime model\n\n`agentic-runtime` executes workflow definitions into durable run records.\n\nExecution contract:\n\n1. Load workflow definition.\n2. Create run record and initial state snapshot.\n3. Execute step-by-step with retry/branch logic.\n4. Persist step records and state versions after each step.\n5. Mark run terminal (`COMPLETED`, `FAILED`, or `COMPLETED_WITH_ERRORS`).\n\nThe runtime is local-first with SQLite persistence.\n\n## 2. Core entities\n\n## `Run`\nTop-level execution record.\n\nKey fields:\n- `run_id`\n- `workflow_id`\n- `workflow_version`\n- `workflow_hash`\n- `workflow_yaml`\n- `workflow_steps`\n- `input_hash`\n- `status`\n- `created_at`, `started_at`, `completed_at`\n- `error`\n\n## `StepExecution`\nOne executed step instance in timeline order.\n\nKey fields:\n- `step_id`, `step_type`\n- `execution_index`\n- `status`\n- `attempt_count`\n- `input`, `output`\n- `error`, `last_error`\n- `state_before`, `state_after`\n- `started_at`, `finished_at`, `duration_ms`\n\n## `RuntimeState`\nState manager abstraction used during execution (instead of ad-hoc raw dict mutation).\n\nAPIs:\n- `get(key)`\n- `set(key, value, step_name=None)`\n- `exists(key)`\n- `delete(key)`\n- `snapshot()`\n- `to_dict()`\n- `set_step_output(step_name, output)`\n- `diff(before, after)`\n\n## 3. State contract\n\nRuntime state shape is namespaced:\n\n```json\n{\n  \"inputs\": {},\n  \"steps\": {},\n  \"runtime\": {}\n}\n```\n\nRules:\n- `inputs`: request/context input payload for the run.\n- `steps.<step_id>`: owned output namespace per step.\n- `runtime`: runtime-level metadata namespace.\n\nWhy this matters:\n- prevents cross-step key collisions\n- preserves step output ownership\n- makes branch and replay analysis tractable\n\n## 4. Workflow model\n\nWorkflows are YAML with identity metadata and ordered `steps`.\n\nWorkflow metadata:\n- `workflow.id`\n- `workflow.version`\n\nWorkflow-level input declarations:\n- `inputs:` declares what the workflow expects from the caller\n- Supports mapping form (with `description`, `required`, `default`) or list shorthand\n- `inputs_contract` (legacy list form) is still supported for backward compatibility\n- Workflows without input declarations infer available inputs from step references\n\nStep types:\n- `model`\n- `tool`\n\nStep controls:\n- `inputs` mapping from state paths (`inputs.issue`, `steps.a.summary`)\n- `inputs` contract list (`[issue, summary]`) for declared read dependencies\n- `outputs` contract list (`[summary, sentiment]`) for declared write boundaries\n- `retry` policy (`attempts`, `backoff`, `initial_delay`)\n- `next` branching rules (`when`/`goto`, optional `default`)\n\nWorkflow-level control:\n- `on_error: fail_fast | continue`\n\nVersioning behavior:\n- `ai run <workflow_id>` resolves latest `vN` from registry scan in `workflows/`\n- `ai run <workflow_id>@<version>` resolves exact version\n- `ai run <path/to/workflow.yaml>` remains supported\n- run record stores workflow snapshot (`workflow_yaml`) for replay stability even if files change later\n\n## 5. Execution engine (`Executor`)\n\n## Handlers (model step functions)\n\nA handler is a Python function that implements the logic for a `model` step.\n\nThe contract:\n```python\nStepHandler = Callable[[RuntimeState], Dict[str, Any]]\n```\n\nA handler receives the step's input as a `RuntimeState` and returns a dict of outputs. The runtime manages everything else \u2014 retries, state snapshots, persistence, resume.\n\nLifecycle:\n1. Workflow YAML declares `handler: <name>` on a `model` step.\n2. At startup, built-in handlers are registered and the `handlers/` directory is scanned for user-defined handlers.\n3. During workflow loading, the name is resolved to the actual function and attached to the `StepDefinition`.\n4. At execution time, `Executor` calls `step_def.handler(step_input_state)` and captures the returned dict.\n5. The output dict is written to `steps.<step_id>` in state.\n\nRegistration (built-in):\n```python\nregistry = StepHandlerRegistry()\nregistry.register(\"generate_summary\", generate_summary)\n```\n\nAuto-discovery from `handlers/` directory:\n```python\nfrom agent_runtime.handler_discovery import register_discovered_handlers\nregister_discovered_handlers(registry, \"handlers\")\n```\n\nDiscovery conventions (per module in `handlers/`):\n1. **Zero-config:** every public function (name not starting with `_`) is registered using the function name.\n2. **Explicit:** define a `__handlers__` dict mapping handler names to functions. This gives full naming control and skips helper functions.\n\nExample handler:\n```python\ndef generate_summary(state: RuntimeState) -> Dict[str, Any]:\n    issue = state[\"issue\"]\n    return {\"summary\": f\"Summary of: {issue}\"}\n```\n\nHandlers are distinct from tools:\n- **Handlers** are plain functions (`Callable[[RuntimeState], dict]`) for `model` steps.\n- **Tools** implement the `Tool` protocol (with `execute`, `input_schema`, etc.) for `tool` steps.\n\nModules:\n- `steps.py` \u2014 `StepHandlerRegistry` and built-in handlers\n- `handler_discovery.py` \u2014 directory-based handler auto-discovery\n\n## Step pointer model\nExecutor uses a pointer (`current_step_id`) rather than list-only traversal.\n\nBenefits:\n- linear flow support\n- conditional branching support\n- consistent resume semantics\n\n## Step execution algorithm\nFor each step:\n\n1. Capture `state_before` snapshot.\n2. Build step input from `inputs` mapping (or snapshot fallback).\n3. Execute with retry policy.\n4. Validate output structure.\n5. Write output to `steps.<step_id>` namespace.\n6. Capture `state_after` snapshot.\n7. Persist `StepExecution` + new state version.\n8. Resolve next step via branch rules or sequential fallback.\n\n## Step contracts\nStep contracts make state boundaries explicit:\n\n- reads = `inputs`\n- writes = `outputs`\n\nValidation at workflow load:\n- future-read detection for contract inputs\n- output collision detection\n\nValidation at runtime:\n- output contract enforcement (missing or undeclared keys fail step)\n\nThis keeps step interfaces stable as workflows grow.\n\n## Branch resolution\n`next` rules are evaluated top-to-bottom:\n- first `when` evaluating `True` wins\n- else `default` if present\n- else branch resolution failure\n\nExpression context is constrained (`state`, `len`) to keep evaluation deterministic and safer.\n\n## 6. Tool subsystem\n\nTools are first-class runtime components.\n\nInterfaces:\n- `Tool`\n- `ToolResult`\n- `RuntimeContext`\n\n`ToolRegistry` stores tool objects, not plain functions.\n\nTool execution path includes:\n- schema validation (`input_schema`)\n- runtime context injection (`run_id`, `step_id`, state, logger)\n- optional per-tool timeout/retries\n- structured tool events:\n  - `TOOL_START`\n  - `TOOL_SUCCESS`\n  - `TOOL_ERROR`\n\n### Tool auto-discovery\n\nThe runtime automatically discovers tools from the `tools/` directory.\n\nDiscovery convention: every class in a module that satisfies the Tool protocol\n(has `name`, `description`, `input_schema`, and `execute`) and whose class name\ndoes not start with `_` is instantiated and registered.\n\nClasses imported from other modules (e.g. base classes) are skipped via `__module__` check.\n\nBuilt-in tools (`tools.echo`, `tools.http`, `tools.file`, `tools.shell`) are always available regardless of what's in `tools/`.\n\n### Built-in tools\n\n| Tool | Name | Description |\n|------|------|-------------|\n| `EchoTool` | `tools.echo` | Returns input message (testing/examples) |\n| `HttpTool` | `tools.http` | HTTP/HTTPS requests with scheme validation |\n| `FileTool` | `tools.file` | Read/write/append/list files (sandboxed to root) |\n| `ShellTool` | `tools.shell` | Execute shell commands with timeout + output capture |\n\nModules:\n- `tools/base.py` \u2014 `Tool` protocol, `ToolResult`, `RuntimeContext`\n- `tools/registry.py` \u2014 `ToolRegistry`\n- `tools/discovery.py` \u2014 `ToolDiscovery`, `discover_tools()`, `register_discovered_tools()`\n- `tools/echo.py` \u2014 built-in `EchoTool`\n- `tools/http.py` \u2014 built-in `HttpTool`\n- `tools/file.py` \u2014 built-in `FileTool`\n- `tools/shell.py` \u2014 built-in `ShellTool`\n- `tools/validation.py` \u2014 input schema validation\n\n## 7. Persistence model (SQLite)\n\nTables:\n- `runs`\n- `steps`\n- `state_versions`\n\nProperties:\n- run metadata is durable\n- step timeline is ordered (`execution_index`)\n- full state evolution is queryable (`state_versions` + `state_before/state_after`)\n\n## 8. Memory subsystem\n\nThe runtime manages four memory tiers through `MemoryManager`:\n\n| Tier | Class | Status | Purpose |\n|------|-------|--------|--------|\n| Working | `WorkingMemory` | **Implemented** | Active context for current execution (scratch store, sliding window, active task) |\n| Episodic | `EpisodicMemory` | **Implemented** | Historical run/interaction log (SQLite-backed) |\n| Semantic | `SemanticMemory` | **Implemented** | Long-term knowledge store (SQLite + FTS5 full-text search) |\n| Procedural | `ProceduralMemory` | Stub | Learned workflows and playbooks (roadmap documented) |\n\nAll tiers implement the `MemoryTier` protocol:\n- `read(context) -> Dict[str, Any]`\n- `write(payload) -> None`\n\n`MemoryManager` orchestrates all tiers:\n- `hydrate_state(state)` \u2014 reads from all tiers into `runtime.memory.<tier>` namespace using deep-merge\n- `persist_state(state)` \u2014 writes state to all tiers after execution\n- `_tiers()` \u2014 yields `(name, tier)` pairs for iteration\n- `_deep_merge()` \u2014 recursive dict merge utility\n\nMemory hooks are invoked at run start (hydrate) and run end (persist).\n\n### Working memory\n\n`WorkingMemory` is an in-memory ephemeral store for the current execution:\n- **Scratch** \u2014 key-value store with byte budget enforcement (`put/get/remove/clear_scratch`)\n- **Entries** \u2014 deque-based sliding window with `max_entries`, auto-eviction\n- **Active task** \u2014 single current objective dict\n- `write()` auto-captures latest step output as a context entry\n- `reset()` clears all state at run end\n- Configurable limits: `working_memory_max_entries`, `working_memory_max_scratch_bytes` in `RuntimeConfig`\n\n### Episodic memory (SQLite)\n\n`EpisodicMemory` stores condensed episode records per run.  It tracks:\n- `workflow_id`, `run_id`, `status`\n- `inputs_summary` (key names), `outputs_summary` (step names)\n- `error` (if failed), `created_at`\n\nQuery API:\n- `record(...)` \u2014 write a single episode row\n- `recall(workflow_id, limit)` \u2014 most recent episodes for a workflow\n- `recall_all(limit)` \u2014 most recent across all workflows\n\n`read()` hydrates `runtime.memory.episodic` with past episodes for the current workflow,\ngiving handlers context about prior runs.\n\nWhen no `db_path` is provided, `EpisodicMemory` operates as an in-memory stub\n(backward compatible with tests and default CLI).\n\n### Semantic memory (SQLite + FTS5)\n\n`SemanticMemory` stores long-term knowledge facts with full-text search:\n- `store(key, content, tags, metadata)` \u2014 insert/update with FTS5 index sync\n- `get(key)` \u2014 exact key lookup\n- `delete(key)` \u2014 removes fact + FTS index entry\n- `search(query, limit)` \u2014 FTS5 MATCH with BM25 ranking, prefix tokenization (`tokenize='porter unicode61'`)\n- `search_by_tags(tags, match_all)` \u2014 tag-based query with match_all/match_any modes\n- `count()` \u2014 total fact count\n\nProtocol-driven: `read()` looks for `runtime.memory.semantic.query` in state; `write()` persists facts from `runtime.memory.semantic.store` list.\n\nFalls back to in-memory dict when no `db_path` is provided.\n\n**Not yet implemented:** vector-similarity retrieval (embeddings + cosine distance) \u2014 roadmap item.\n\n### Procedural memory (stub)\n\n`ProceduralMemory` is currently a stub. Roadmap: mine episodic history for patterns, store rules in SQLite, confidence scoring, LLM-assisted extraction.\n\nModules:\n- `memory/base.py` \u2014 `MemoryTier` protocol and `MemoryManager`\n- `memory/working.py` \u2014 `WorkingMemory`\n- `memory/episodic.py` \u2014 `EpisodicMemory`\n- `memory/semantic.py` \u2014 `SemanticMemory`\n- `memory/procedural.py` \u2014 `ProceduralMemory`\n\n## 9. Workflow registry and version resolution\n\n`WorkflowRegistry` resolves workflow references to loaded workflow definitions.\n\nResolution rules:\n- `ai run <workflow_id>` \u2014 scans `workflows/**/*.yaml` and `*.yml`, selects highest `vN` for matching `workflow.id`\n- `ai run <workflow_id>@<version>` \u2014 resolves exact version\n- `ai run <path>` \u2014 direct file load (no registry scan)\n\nKey classes:\n- `WorkflowRegistry` \u2014 scans directories, registers workflows, resolves by id/version\n- `WorkflowReference` \u2014 frozen dataclass with `workflow_id` and `version`\n- `parse_workflow_reference(value)` \u2014 parses `\"id\"` or `\"id@version\"` strings\n\nModule: `workflow_registry.py`\n\n## 10. Error hierarchy\n\nAll runtime exceptions inherit from `RuntimeErrorBase(Exception)`:\n\n- `WorkflowValidationError` \u2014 invalid workflow YAML (bad step ids, missing targets, invalid retry)\n- `StepExecutionError` \u2014 step handler or tool failure during execution\n- `ToolNotFoundError` \u2014 tool name not registered in `ToolRegistry`\n- `HandlerNotFoundError` \u2014 handler name not registered in `StepHandlerRegistry`\n- `BranchResolutionError` \u2014 no matching `when` rule and no `default`\n- `RunNotFoundError` \u2014 run id not found in storage\n- `ReplayDataMissingError` \u2014 incomplete step/state data for replay\n- `ReplayMismatchError` \u2014 reconstructed state diverges from recorded state during `--verify-state`\n- `WorkflowIntegrityError` \u2014 workflow YAML has been modified since the original run (blocks resume)\n\nModule: `errors.py`\n\n## 11. Logging and utilities\n\n### Structured logger\n\n`StructuredLogger` emits JSON events to stderr:\n- `info(event, payload)` \u2014 informational events\n- `error(event, payload)` \u2014 error events\n- `from_dataclass(event, obj)` \u2014 serializes dataclass objects into log entries\n\nModule: `logging.py`\n\n### Utilities\n\nKey functions in `utils.py`:\n- `utc_now()` \u2014 timezone-aware UTC timestamp\n- `sha256_text(text)` / `sha256_json(data)` \u2014 deterministic hashing for workflow and input fingerprinting\n- `resolve_path(path, state)` \u2014 resolves dot-notation paths like `steps.generate_summary.summary`\n- `build_step_input(input_spec, state)` \u2014 materializes step input from state path mapping\n- `safe_eval(expr, state)` \u2014 constrained expression evaluation for branch conditions (AST-validated, allows only `state` and `len`)\n- `format_template(value, state)` \u2014 recursive template resolution in input specs\n\n## 12. Resume semantics\n\n`ai resume <run_id>`:\n\n1. Validate run is resumable (`FAILED` only).\n2. **Validate workflow hash** \u2014 the runtime compares the stored workflow hash\n   against the current workflow hash and raises `WorkflowIntegrityError` if they\n   differ.  This prevents resuming a run whose workflow definition has been\n   modified since the original execution.\n3. Determine resume step from recorded history.\n4. Load latest state snapshot.\n5. Continue execution from resume step.\n\nDeterminism principle:\n- completed history is preserved\n- resumed traversal uses same branch/step resolution logic\n\n## 13. Replay semantics\n\n`ai replay <run_id>` is simulation, not execution.\n\nReplay engine:\n- loads run + step history + initial state\n- replays step timeline by injecting recorded state transitions\n- does not call handlers/tools/models\n- optional `--verify-state` checks reconstructed state against `state_before`\n\nUse this for postmortems and reproducibility checks.\n\n## 14. Observability surfaces\n\nCLI modes:\n- `inspect` summary\n- `inspect --steps` step-centric details\n- `inspect --state-history` state timeline and diff markers\n- `state-diff` deep key-path state changes\n- `visualize` run visualization:\n  - `--ascii`\n  - `--timeline`\n  - default HTML (`.runs/<run_id>/visualization.html`)\n\nDiff markers:\n- `+` added\n- `-` removed\n- `~` changed\n\nVisualization internals:\n- `visualization/run_loader.py`: loads run, steps, state, workflow metadata\n- `visualization/graph_builder.py`: execution nodes/edges + branch decision reconstruction\n- `visualization/timeline_builder.py`: state deltas + step timing/tool detail timeline\n- `visualization/ascii_renderer.py`: terminal-friendly view\n- `visualization/html_renderer.py`: local standalone HTML report\n\n## 15. Determinism guardrails (current)\n\nImplemented:\n- workflow id/version/hash snapshot on every run\n- workflow hash + input hash capture\n- step output namespacing\n- persisted execution ordering\n- state snapshots for before/after\n- replay from persisted history\n\nKnown limitations:\n- no full event-sourcing ledger\n- no idempotency keys for side-effect tools\n- no loop scheduler/loop detector beyond current guards\n\n## 16. Runtime configuration\n\nThe runtime loads settings from `runtime.yaml` (if present), falling back to built-in defaults.\n\nPrecedence (highest wins):\n```\nCLI flag  >  runtime.yaml  >  built-in default\n```\n\nConfig fields:\n- `db_path` \u2014 SQLite database path (default: `runtime.db`)\n- `workflows_dir` \u2014 workflow YAML directory (default: `workflows`)\n- `handlers_dir` \u2014 handler discovery directory (default: `handlers`)\n- `tools_dir` \u2014 tool discovery directory (default: `tools`)\n- `model` \u2014 model backend settings (placeholder for LLM integration)\n- `logging.level` / `logging.format` \u2014 log configuration\n- `overwrite_policy` \u2014 state overwrite behavior: `warn` (default), `strict`, `allow`\n- `working_memory_max_entries` \u2014 max sliding window entries (default: 50)\n- `working_memory_max_scratch_bytes` \u2014 max scratch store size (default: 256KB)\n- `shell_allowlist` / `shell_denylist` \u2014 regex patterns for ShellTool command filtering\n- `default_llm_provider` \u2014 fallback provider for ambiguous model references\n- `llm:` \u2014 provider configuration block (providers, models, API key env vars)\n\nModule: `config.py` \u2014 `RuntimeConfig`, `load_config()`, `apply_cli_overrides()`\n\n## 17. Lifecycle hooks\n\nThe Executor emits structured events at five lifecycle points via an optional `EventCallback`:\n\n```python\nEventCallback = Callable[[str, Dict[str, Any]], None]\n```\n\nEvents:\n| Event | When | Payload includes |\n|-------|------|------------------|\n| `RUN_START` | Run begins | `run_id`, `workflow_id` |\n| `STEP_START` | Step begins | `run_id`, `step_id`, `step_type` |\n| `STEP_COMPLETE` | Step succeeds | `run_id`, `step_id`, `duration_ms`, `handler_duration_ms`/`tool_duration_ms` |\n| `STEP_ERROR` | Step fails (after retries) | `run_id`, `step_id`, `error` |\n| `RUN_COMPLETE` | Run finishes | `run_id`, `status`, `total_duration_ms` |\n\nUsage: pass `on_event` to `Executor.__init__()` or to `run_workflow()`.\n\n## 18. Timing telemetry\n\nPer-step timing captures both total step duration and call-specific latency:\n- `duration_ms` \u2014 total time from state_before snapshot to state_after persistence\n- `handler_duration_ms` \u2014 time spent inside the handler callable (model steps)\n- `tool_duration_ms` \u2014 time spent inside the tool's `execute()` method (tool steps)\n\nRun-level duration is derived from `started_at` / `completed_at` timestamps.\n\nVisualization renderers (HTML, ASCII) and CLI progress output surface these metrics.\n\n## 19. Extension points\n\nDesigned for future expansion:\n- stronger expression engine for branching\n- DAG scheduler and parallel step execution\n- typed state schemas\n- tool permissions and sandbox policies\n- state redaction and compression for large payloads\n- LLM streaming / token-level events\n- multi-agent composition (sub-workflow invocation)\n- PostgreSQL storage backend\n- OpenTelemetry / Prometheus observability\n\n## 20. LLM registry\n\nThe runtime includes a provider registry for managing LLM backends.\n\nKey classes (module `llm/`):\n- `LLMProvider` \u2014 one provider (name, API key env var, base URL, model configs)\n- `ModelConfig` \u2014 one model definition (model_id, temperature, max_tokens, extras)\n- `LLMRegistry` \u2014 central lookup of providers and models\n\nCredential management:\n- API keys are resolved from environment variables at call time \u2014 never stored on disk.\n- `provider.resolve_api_key()` reads `os.environ[api_key_env]`.\n- `registry.check_credentials()` returns a quick health map.\n\nConfiguration (in `runtime.yaml`):\n```yaml\nllm:\n  providers:\n    openai:\n      api_key_env: OPENAI_API_KEY\n      models:\n        gpt-4:\n          temperature: 0.2\n          max_tokens: 4096\n    anthropic:\n      api_key_env: ANTHROPIC_API_KEY\n      models:\n        claude-3-opus:\n          temperature: 0.3\n    local:\n      api_key_env: LOCAL_LLM_KEY\n      base_url: http://localhost:8080/v1\n      models:\n        llama-3:\n          temperature: 0.5\n          max_tokens: 2048\n```\n\nThe registry is loaded from the `llm` section of `RuntimeConfig` during startup\nand is available for handlers to query which model to use.\n\n### LLM call pipeline\n\n`LLMClient` routes requests through provider-specific adapters:\n\n```\nworkflow YAML (handler: llm, model, prompt)\n  \u2192 make_llm_handler() \u2192 LLMClient.call()\n    \u2192 resolve provider/model\n    \u2192 adapter.call() (HTTP to provider API)\n    \u2192 LLMResponse (text, usage, raw)\n```\n\nAdapters:\n- `OpenAIAdapter` \u2014 Chat Completions API (`/v1/chat/completions`)\n- `AnthropicAdapter` \u2014 Messages API (`/v1/messages`)\n\nBoth use stdlib `urllib` (zero additional dependencies).\n\nBuilt-in `llm` handler:\n- Registered as `handler: llm` in workflow YAML\n- Reads `model`, `prompt`, `system`, `provider`, `temperature`, `max_tokens` from step definition\n- Supports `{{ path }}` template syntax in prompts\n- Returns response text under configurable key (default: `text`)\n- Optional `include_metadata: true` captures provider, model, and token usage in output\n\n> **Status: Implemented** \u2014 registry, config loading, credential resolution,\n> OpenAI adapter, Anthropic adapter, LLM client, and built-in handler are\n> functional.  Adapters are synchronous (stdlib `urllib`); async adapters\n> for concurrent execution are a future enhancement.\n\n## 19. Agent manifest system\n\nAn agent manifest (`agent.yaml`) is the portable unit of the runtime.  It\ndeclares everything an agent needs to run \u2014 workflow, handlers, tools, LLM\nproviders, and environment variables \u2014 in a single file.\n\nManifests live in the `agents/` directory.  Multiple agents can coexist\nin one project, sharing handlers and tools.\n\nKey classes (module `agent/`):\n- `AgentManifest` \u2014 parsed manifest dataclass\n- `ProviderRequirement` \u2014 LLM provider + models the agent depends on\n- `ValidationResult` \u2014 one pre-flight check result\n\n### Manifest schema\n\n```yaml\nagent:\n  id: triage_agent\n  version: v2\n  description: \"Triages incoming issues by severity\"\n  runtime: \">=0.1\"\n\nworkflow: workflows/triage.yaml\n\nhandlers:\n  - handlers/classify.py\n  - handlers/summarize.py\n\ntools:\n  - tools/github_tool.py\n\nproviders:\n  - name: openai\n    models: [gpt-4, gpt-4o-mini]\n\nenv:\n  - GITHUB_TOKEN\n\ndefaults:\n  issue: \"unspecified\"\n```\n\n### CLI commands\n\n- `ai validate <manifest>` \u2014 pre-flight checks (files, providers, env vars)\n- `ai export <manifest> [-o output.tar.gz]` \u2014 bundle into portable archive\n- `ai import <archive> [--path .]` \u2014 extract into project, place manifest in `agents/`\n- `ai list` \u2014 list all agents in `agents/`\n- `ai run <agent_id>[@version]` \u2014 resolves from `agents/`, merges defaults with `-i` overrides\n\n### Agent-aware run resolution\n\nWhen `ai run <ref>` is invoked, the runtime first checks `agents/` for a\nmatching `agent.id`.  If found, the manifest's workflow path is used and\ndefault inputs are merged (CLI `-i` overrides take precedence).  If no\nagent matches, the runtime falls back to workflow resolution (file path or\nworkflow registry).\n\n### Export / Import flow\n\n**Export** bundles `agent.yaml` + workflow + handlers + tools into a\nself-contained `.tar.gz` archive.  The archive does not include\n`runtime.yaml` (machine-specific) or the runtime itself.\n\n**Import** extracts the archive into the project tree and places the\nmanifest in `agents/`.  Post-import validation reports missing providers\nand env vars so the operator can configure the environment.\n\n> **Status: Implemented** \u2014 manifest loading, validation, export, import,\n> CLI commands, and agent-aware run resolution are functional.\n> TODO: Support multiple workflows per agent with a designated entry point.\n\n## 20. Status summary\n\n| Capability | Status |\n|---|---|\n| Core execution engine | Implemented |\n| Retry / backoff | Implemented |\n| Conditional branching | Implemented |\n| SQLite persistence | Implemented |\n| Resume from failure | Implemented |\n| Workflow hash lock on resume | Implemented |\n| Deterministic replay | Implemented |\n| State diff & visualization | Implemented |\n| Workflow versioning & registry | Implemented |\n| Step contracts | Implemented |\n| Tool subsystem & discovery | Implemented |\n| Handler auto-discovery | Implemented |\n| LLM provider registry | Implemented |\n| Credential resolution (env vars) | Implemented |\n| Agent manifest system | Implemented |\n| Agent validate / export / import | Implemented |\n| Agent-aware run resolution | Implemented |\n| LLM API call adapters | Implemented |\n| Anthropic adapter | Implemented |\n| Async executor | Implemented |\n| Built-in tools (HTTP, File, Shell) | Implemented |\n| Episodic memory (SQLite) | Implemented |\n| Memory tiers (working, semantic, procedural) | Scaffolding |\n| Multi-workflow agents | Planned |\n| DAG / parallel execution | Planned |\n| Tool permissions / sandboxing | Planned |\n| Idempotency keys | Planned |\n| Event sourcing ledger | Planned |\n", "about/gaps_2026-03-17.md": "# agentic-runtime \u2014 Gap Analysis\n\n**Date:** 2026-03-17\n\n---\n\n## Context\n\nThe runtime has a solid execution engine: runs, steps, retries, branching, resume, replay, persistence, visualization, agent manifest packaging, LLM provider registry, built-in LLM handler, and a full CLI. All of that is implemented and tested where noted.\n\nThis document identifies what is **missing or incomplete** \u2014 the gaps between the current codebase and a product that developers can use to run real AI agents.\n\nGaps are ranked by severity: how much they block real-world adoption.\n\n**Updated 2026-03-18** \u2014 gaps 3, 5, and 6 have been resolved. Gap 2 partially addressed with lifecycle hooks.\n\n---\n\n## Gap 1: LLM quickstart is not the default (High)\n\n**Impact:** Users still experience stub-like workflows by default.\n\nLLM calls and the built-in `handler: llm` exist, but `ai init` and sample workflows still showcase stub handlers. A new user can configure providers and still not see a real LLM call without editing YAML and code.\n\n**What's needed:**\n- Update `ai init` to scaffold a working LLM workflow\n- Add a sample that uses `handler: llm` with a real prompt\n- Add a \u201cquickstart\u201d path that runs an LLM step and opens a visualization\n\n**Files involved:** `cli.py`, `llm/handler.py`, `workflows/samples/`\n\n---\n\n## Gap 2: No streaming or progress feedback (High)\n\n**Impact:** Poor UX for long-running LLM or tool steps.\n\n**Status: Partially resolved (2026-03-17/18).** Lifecycle hooks (`EventCallback`) now emit events at RUN_START, STEP_START, STEP_COMPLETE, STEP_ERROR, RUN_COMPLETE. CLI shows real-time step progress. Per-step timing telemetry (handler_duration_ms, tool_duration_ms) added. **Remaining:** streaming token output from LLM calls \u2014 requires adapter-level chunked response parsing.\n\n---\n\n## Gap 3: Memory tiers are incomplete and unsafe to hydrate (High)\n\n**Status: RESOLVED (2026-03-17).** \n\n- MemoryManager now uses namespaced deep-merge under `runtime.memory.<tier>` \u2014 state corruption eliminated.\n- WorkingMemory fully implemented: scratch store (byte budget), sliding window entries, active task tracking.\n- SemanticMemory fully implemented: SQLite + FTS5, CRUD, full-text search, tag retrieval.\n- ProceduralMemory remains a stub with documented roadmap.\n\n---\n\n## Gap 4: Sync API cannot run inside existing event loops (Medium)\n\n**Impact:** Embedding the runtime in async apps is awkward.\n\nThe sync entrypoints call `asyncio.run()`, which fails if a loop is already running (FastAPI, Jupyter). Users must remember to call the async API.\n\n**What's needed:**\n- A safe sync wrapper that works inside running loops\n- Or an explicit async-first SDK surface with clear guidance\n\n**Files involved:** `core.py`\n\n---\n\n## Gap 5: CLI input type handling (Medium)\n\n**Status: RESOLVED (2026-03-17).** `_coerce_value()` auto-parses `-i key=value` args: bool \u2192 int \u2192 float \u2192 JSON \u2192 str fallback. Workflow input declarations support `description`, `required`, `default` for type documentation.\n\n---\n\n## Gap 6: Security hardening TODOs (Medium)\n\n**Status: RESOLVED (2026-03-17).**\n\n- `FileTool._safe_path` prefix bypass fixed \u2014 uses `os.sep` suffix check.\n- `import_agent` rejects symlinks/hardlinks in tar archives.\n- Manifest paths validated against project root via `_safe_copy`.\n- `ShellTool` allowlist/denylist with regex-based command filtering.\n- `safe_eval` blocks dunder attribute access.\n\n---\n\n## Gap 7: Minimal expression language for branching (Low-medium)\n\n**Impact:** Forces workarounds for non-trivial conditions.\n\nBranch conditions use `safe_eval` restricted to `state` and `len`. This limits string operations, membership tests, and complex predicates.\n\n**What's needed:**\n- Expand allowed expressions or add a safe helper library\n- Preserve determinism and avoid unsafe execution\n\n---\n\n## Gap 8: No multi-agent or sub-workflow composition (Low)\n\n**Impact:** Limits orchestrator patterns to single workflows.\n\nEach agent runs one workflow. No mechanism for:\n- One agent calling another agent\n- A step invoking a sub-workflow\n- Workflow composition\n\n---\n\n## Gap 9: No authentication or authorization (Low)\n\n**Impact:** Blocks shared/production use.\n\nNo access control exists. Anyone with CLI access can run agents, read runs, and resume runs.\n\n---\n\n## Recommended priority order\n\nFor shipping to early adopters:\n\n| Priority | Gap | Rationale |\n|---|---|---|\n| **P0** | Gap 1: LLM quickstart is not the default | First-time users must see a real LLM call without editing code |\n| **P1** | Gap 2: Streaming/progress feedback | Partially done \u2014 lifecycle hooks + CLI progress. LLM streaming remains |\n| ~~P1~~ | ~~Gap 3: Memory safety and tier clarity~~ | **RESOLVED** |\n| **P2** | Gap 4: Async embedding | Important for SDK adoption in real apps |\n| ~~P2~~ | ~~Gap 5: Input type coercion~~ | **RESOLVED** |\n| ~~P2~~ | ~~Gap 6: Security hardening~~ | **RESOLVED** |\n| **P3** | Gap 7: Richer expression language | Convenience, not blocking |\n| **P3** | Gap 8: Multi-agent composition | V2 feature |\n| **P3** | Gap 9: Auth model | Only needed for shared/production deployment |\n\n---\n\n*Produced from full codebase audit on 2026-03-17.*\n", "about/git_runtime_date.md": "# Runtime Positioning Notes \u2014 2026-03-17\n\n## Goal\n\nPosition `agentic-runtime` as the execution runtime for agents, not just a history system. The runtime should feel like an active control plane that runs, governs, and observes agent workloads.\n\n## Why It Currently Reads As \u201cGit For Agents\u201d\n\n1. The public narrative highlights replay, resume, and inspection more than live execution.\n2. The CLI surface is strong on forensics (`inspect`, `replay`, `visualize`) and light on operational control (`serve`, `schedule`, `cancel`, `monitor`).\n3. Quickstart and samples still lead with stub handlers, which hides real execution and makes the system feel like a recorder, not a runner.\n\n## What A Runtime Must Feel Like\n\n1. Always-on execution with a worker or server mode.\n2. Operational control primitives: cancel, pause, retry, prioritize.\n3. Resource governance: timeouts, rate limits, concurrency limits.\n4. Observability: live events, streaming output, and metrics.\n5. Safety and isolation: tool sandboxing, secrets, input policy checks.\n6. Programmatic embed: a first-class SDK API, not just a CLI.\n\n## Product Shifts To Align With \u201cRuntime\u201d\n\n1. Add `ai serve` or `ai worker` to accept runs over HTTP or a local queue.\n2. Add cancellation and scheduling primitives to the CLI and SDK.\n3. Add streaming and real-time execution events.\n4. Make the default scaffold run a real LLM step with a single command.\n5. Introduce an SDK wrapper for run, resume, replay, and events.\n\n## Messaging Update\n\nPositioning statement draft:\n\n\u201c`agentic-runtime` is the execution runtime for AI agents. It runs workflows deterministically, enforces state contracts, and provides operational control and observability so agent systems can be debugged, resumed, and trusted in production.\u201d\n\n## Near-Term Deliverables\n\n1. Update `ai init` to scaffold a real LLM workflow.\n2. Add a `ai quickstart` command that runs and visualizes an LLM step.\n3. Add an event hook or streaming channel for step and LLM progress.\n4. Document the runtime control plane: run, cancel, resume, replay, inspect.\n\n", "about/status_2026-03-17.md": "# agentic-runtime \u2014 Status Report\n\n**Date:** 2026-03-17 (updated 2026-03-18)\n\n---\n\n## Summary\n\n`agentic-runtime` is a runtime for AI agents \u2014 not a framework or library, but the execution substrate agents run on. It provides deterministic execution, full state tracking, failure recovery, and portable agent packaging.\n\nThis document captures the current state of every subsystem, what is implemented, what is scaffolding, and what is planned.\n\n---\n\n## 1. Architecture overview\n\nThe runtime executes YAML workflow definitions into durable run records with SQLite persistence. The execution model is step-sequential with conditional branching, retry/backoff, and deterministic resume/replay.\n\n**Core flow:**\n1. Load workflow definition (from file, registry, or agent manifest)\n2. Create run record + initial state snapshot\n3. Execute steps sequentially with retry/branch logic\n4. Persist step records and state versions after each step\n5. Mark run terminal (COMPLETED, FAILED, or COMPLETED_WITH_ERRORS)\n\n---\n\n## 2. Module inventory\n\n### Core modules (`src/agent_runtime/`)\n\n| Module | Purpose | Status |\n|---|---|---|\n| `core.py` | Executor engine, Run/StepExecution/StepDefinition datamodels, retry/branch/tool dispatch | Implemented |\n| `cli.py` | `ai` CLI \u2014 init, run, inspect, resume, replay, state-diff, visualize, validate, export, import, list | Implemented |\n| `config.py` | RuntimeConfig from `runtime.yaml`, CLI overrides, LLM registry wiring | Implemented |\n| `errors.py` | Exception hierarchy (10 types) under `RuntimeErrorBase` | Implemented |\n| `state.py` | `RuntimeState` \u2014 namespaced state manager with diff/snapshot | Implemented |\n| `steps.py` | `StepHandlerRegistry` + 5 built-in handlers | Implemented |\n| `workflow.py` | YAML workflow loader/normalizer, step contract validation | Implemented |\n| `workflow_registry.py` | Version-aware workflow resolution from directory | Implemented |\n| `handler_discovery.py` | Auto-discover handlers from `handlers/` directory | Implemented |\n| `resume.py` | Resume point determination and validation | Implemented |\n| `replay.py` | Deterministic run replay from persisted history | Implemented |\n| `logging.py` | Structured JSON logger (stderr) | Implemented |\n| `utils.py` | Hashing, path resolution, safe_eval, template formatting | Implemented |\n\n### Agent subsystem (`src/agent_runtime/agent/`)\n\n| Module | Purpose | Status |\n|---|---|---|\n| `manifest.py` | `AgentManifest` dataclass, `load_agent_manifest()`, `validate_agent()`, `ValidationResult` | Implemented |\n| `packaging.py` | `export_agent()`, `import_agent()` with tar.gz archives and path traversal protection | Implemented |\n\n### LLM subsystem (`src/agent_runtime/llm/`)\n\n| Module | Purpose | Status |\n|---|---|---|\n| `registry.py` | `LLMProvider`, `ModelConfig`, `LLMRegistry` \u2014 multi-provider management, env-var credential resolution, YAML config loading | Implemented |\n| `client.py` | `LLMClient` routing to provider adapters with logging | Implemented |\n| `adapters.py` | OpenAI, Anthropic, and Gemini HTTP adapters | Implemented |\n| `handler.py` | Built-in `handler: llm` for workflow steps | Implemented |\n| `types.py` | Shared response types | Implemented |\n\n### Memory subsystem (`src/agent_runtime/memory/`)\n\n| Module | Purpose | Status |\n|---|---|---|\n| `base.py` | `MemoryTier` protocol, `MemoryManager` with namespaced deep-merge hydration | Implemented |\n| `working.py` | `WorkingMemory` \u2014 scratch store (byte budget), sliding window entries, active task | Implemented |\n| `episodic.py` | `EpisodicMemory` \u2014 SQLite-backed run summaries | Implemented |\n| `semantic.py` | `SemanticMemory` \u2014 SQLite + FTS5, full-text search, tags, CRUD | Implemented |\n| `procedural.py` | `ProceduralMemory` \u2014 learned workflows and playbooks | Stub (roadmap documented) |\n\nAll implemented tiers hydrate under `runtime.memory.<tier>` using deep-merge. State corruption risk from the original shallow `dict.update` has been eliminated.\n\n### Storage subsystem (`src/agent_runtime/storage/`)\n\n| Module | Purpose | Status |\n|---|---|---|\n| `base.py` | Abstract `Storage` interface | Implemented |\n| `sqlite.py` | `SQLiteStorage` \u2014 runs, steps, state_versions tables | Implemented |\n\n### Tool subsystem (`src/agent_runtime/tools/`)\n\n| Module | Purpose | Status |\n|---|---|---|\n| `base.py` | `Tool` protocol, `ToolResult`, `RuntimeContext` | Implemented |\n| `registry.py` | `ToolRegistry` \u2014 store/lookup tool instances | Implemented |\n| `discovery.py` | Auto-discover tools from `tools/` directory | Implemented |\n| `echo.py` | Built-in `EchoTool` | Implemented |\n| `http.py` | Built-in `HttpTool` | Implemented |\n| `file.py` | Built-in `FileTool` | Implemented |\n| `shell.py` | Built-in `ShellTool` | Implemented |\n| `validation.py` | JSON Schema input validation for tools | Implemented |\n\n### Visualization subsystem (`src/agent_runtime/visualization/`)\n\n| Module | Purpose | Status |\n|---|---|---|\n| `run_loader.py` | Load run/step/state/workflow data for visualization | Implemented |\n| `graph_builder.py` | Execution graph nodes/edges + branch reconstruction | Implemented |\n| `timeline_builder.py` | State deltas + step timing timeline | Implemented |\n| `ascii_renderer.py` | Terminal-friendly run visualization | Implemented |\n| `html_renderer.py` | Standalone HTML report | Implemented |\n\nNote: Visualizations now include run-level timing (start, completion, total duration),\nper-step call timing (tool/LLM latency) alongside step durations, and\n`ai visualize --html` auto-opens the browser by default (added 2026-03-18).\n\n---\n\n## 3. CLI commands\n\n| Command | Purpose | Status |\n|---|---|---|\n| `ai init [--path DIR]` | Scaffold project (workflows/, handlers/, tools/, agents/, runtime.yaml) | Implemented |\n| `ai onboard [--path DIR]` | Guided setup wizard (init + provider config + sample) | Implemented |\n| `ai setup [--path DIR] [--check]` | Configure providers/keys, verify credentials | Implemented |\n| `ai run <ref> [-i KEY=VAL]` | Run workflow or agent (agent-aware resolution from agents/) | Implemented |\n| `ai inspect <run_id> [--steps] [--state-history]` | Inspect run details | Implemented |\n| `ai resume <run_id> [--workflow PATH]` | Resume failed run with workflow hash lock | Implemented |\n| `ai replay <run_id> [--verify-state] [--until STEP] [--step-by-step]` | Deterministic replay | Implemented |\n| `ai state-diff <run_id> [--step STEP]` | Deep state change diffs | Implemented |\n| `ai visualize <run_id> [--ascii|--html|--timeline]` | Run visualization | Implemented |\n| `ai validate <manifest>` | Pre-flight agent manifest checks | Implemented |\n| `ai export <manifest> [-o FILE]` | Export agent as .tar.gz | Implemented |\n| `ai import <archive> [--path DIR]` | Import agent archive | Implemented |\n| `ai list [--agents-dir DIR]` | List available agents | Implemented |\n\n---\n\n## 4. Error hierarchy\n\nAll exceptions inherit from `RuntimeErrorBase(Exception)`:\n\n| Exception | Trigger |\n|---|---|\n| `WorkflowValidationError` | Invalid workflow YAML |\n| `StepExecutionError` | Step handler/tool failure |\n| `ToolNotFoundError` | Tool name not in registry |\n| `HandlerNotFoundError` | Handler name not in registry |\n| `BranchResolutionError` | No matching branch rule and no default |\n| `RunNotFoundError` | Run ID not in storage |\n| `ReplayDataMissingError` | Incomplete data for replay |\n| `ReplayMismatchError` | Replayed state diverges from recorded |\n| `WorkflowIntegrityError` | Workflow YAML changed since original run (blocks resume) |\n| `AgentValidationError` | Agent manifest invalid or missing dependencies |\n\n---\n\n## 5. Key features detail\n\n### 5.1 Workflow hash lock on resume\n\nWhen a run is created, the workflow YAML hash is stored in the `Run` record. On `ai resume`, `Executor.resume()` compares the stored hash against the current workflow hash and raises `WorkflowIntegrityError` if they differ. This prevents resuming against a modified workflow.\n\n**Files:** [core.py](../src/agent_runtime/core.py) (`Executor.resume()`), [errors.py](../src/agent_runtime/errors.py)\n\n### 5.2 LLM calls and built-in handler\n\nThe runtime now includes an `LLMClient`, provider adapters, and a built-in `handler: llm` that turns workflow YAML directly into API calls.\n\n**Capabilities:**\n- Register/lookup providers and models\n- Credential health check (`check_credentials()`)\n- Load from `runtime.yaml` `llm:` section\n- OpenAI, Anthropic, and Gemini HTTP adapters\n- Built-in `llm` handler with prompt templating and optional metadata\n\n**Known gaps:** streaming output, Responses API support, and E2E tests for the LLM handler/adapter path.\n\n**Files:** [llm/registry.py](../src/agent_runtime/llm/registry.py), [llm/client.py](../src/agent_runtime/llm/client.py), [llm/adapters.py](../src/agent_runtime/llm/adapters.py), [llm/handler.py](../src/agent_runtime/llm/handler.py)\n\n### 5.3 Agent manifest system\n\nThe portable unit of the runtime. An `agent.yaml` declares everything an agent needs: workflow, handlers, tools, LLM providers, env vars, and default inputs.\n\n**Capabilities:**\n- Load and parse manifests (`load_agent_manifest()`)\n- Pre-flight validation: file existence, provider availability, env vars, credential checks\n- Export as `.tar.gz` archive (manifest + workflow + handlers + tools)\n- Import from archive with post-import validation\n- Agent-aware run resolution: `ai run <agent_id>[@version]`\n- Default input merging (manifest defaults < CLI `-i` overrides)\n- Path traversal protection in import\n\n**Files:** [agent/manifest.py](../src/agent_runtime/agent/manifest.py), [agent/packaging.py](../src/agent_runtime/agent/packaging.py), [cli.py](../src/agent_runtime/cli.py)\n\n### 5.4 State model\n\nNamespaced state (`inputs`, `steps`, `runtime`) prevents cross-step key collisions and preserves ownership. `RuntimeState` manages reads, writes, snapshots, and diffs.\n\n### 5.5 Retry and backoff\n\nPer-step retry policy: configurable `attempts`, `backoff` (fixed/exponential), `initial_delay`. Retry state (`attempt_count`, `last_error`) is captured in `StepExecution`.\n\n### 5.6 Conditional branching\n\n`next` rules with `when` expressions evaluated top-to-bottom. Expression evaluation is constrained via AST validation (only `state` and `len` allowed). Default fallback supported.\n\n### 5.7 Step contracts\n\nDeclared `inputs` (reads) and `outputs` (writes) per step. Workflow-load-time validation catches future-read and output collision errors. Runtime enforcement checks output keys against declared contract.\n\n### 5.8 Deterministic replay\n\n`ai replay` reproduces a historical run by injecting recorded state transitions without calling handlers/tools. Optional `--verify-state` checks reconstructed state matches recorded `state_before`.\n\n### 5.9 Visualization\n\nThree modes: ASCII (terminal), HTML (browser), and timeline (text). Powered by graph/timeline builders that reconstruct execution flow and state deltas from persisted data.\n\n---\n\n## 6. Test coverage\n\n| Test file | Scope |\n|---|---|\n| `test_runtime.py` | Core executor: run, state, step execution |\n| `test_branching.py` | Conditional branching with `next` rules |\n| `test_branch_resume.py` | Resume from branched execution paths |\n| `test_resume.py` | Resume semantics and validation |\n| `test_retry_policy.py` | Retry/backoff behavior |\n| `test_replay.py` | Deterministic replay subsystem |\n| `test_state_manager.py` | `RuntimeState` operations |\n| `test_state_diff.py` | State diff computation |\n| `test_state_history.py` | State history tracking |\n| `test_step_contracts.py` | Input/output contract enforcement |\n| `test_visualization.py` | Visualization builders and renderers |\n| `test_workflow_versioning.py` | Workflow registry and version resolution |\n| `test_workflow_lock.py` | Workflow hash lock on resume |\n| `test_llm_registry.py` | LLM provider registry |\n| `test_agent_manifest.py` | Agent manifest: load, validate, export, import |\n\n---\n\n## 7. Sample workflows\n\nLocated in `workflows/samples/`:\n\n| File | Demonstrates |\n|---|---|\n| `01_linear_issue_summary.yaml` | Linear step execution |\n| `02_retry_and_backoff.yaml` | Retry policy with exponential backoff |\n| `03_branching_triage.yaml` | Conditional branching by severity |\n| `04_fail_and_resume.yaml` | Deliberate failure for resume testing |\n| `versioning/code_review_agent_v1.yaml` | Workflow versioning (v1) |\n| `versioning/code_review_agent_v2.yaml` | Workflow versioning (v2) |\n\n---\n\n## 8. Configuration\n\n`runtime.yaml` controls runtime defaults: SQLite path, workflow directories, logging, and LLM provider configuration. Providers are registered under the `llm:` section and API keys are resolved from environment variables at runtime.\n", "changelog/CHANGELOG_2026-03-16.md": "# Changelog \u2014 2026-03-16\n\n## Added\n- LLM client and OpenAI adapter with structured logging hooks and normalized responses.\n- Built-in `llm` handler with state-based prompt templating.\n- Workflow parsing support for `handler: llm` step fields (`model`, `prompt`, `system`, and related params).\n- `{{ path }}` state template rendering helper.\n\n## Changed\n- Executor now supports async-native execution with sync wrappers that refuse to run inside an active event loop.\n- Tool execution path is async to avoid `asyncio.run()` in embedded contexts.\n\n## Tests\n- Added coverage for async run execution and loop-guard behavior.\n", "changelog/CHANGELOG_2026-03-17.md": "# Changelog \u2014 2026-03-17\n\n## Bug Fixes\n\n### P0 \u2014 Correctness\n\n- **`validate_input` required-field enforcement** (`tools/validation.py`):\n  The JSON Schema validator ignored `required` arrays entirely. Tools declaring required fields (HttpTool: `url`, FileTool: `action`/`path`) accepted empty payloads and failed with confusing KeyErrors downstream. Now enforces `required` before type-checking, raising `ValueError(\"Missing required field: '<key>'\")`.\n\n- **`append_step` truthiness bug** (`storage/sqlite.py`):\n  Empty dict payloads `{}` for `input`, `output`, `state_before`, and `state_after` were stored as `NULL` in SQLite due to `if step.X else None` treating `{}` as falsy. Changed to `if step.X is not None else None`. Empty dicts now roundtrip correctly, fixing replay accuracy and state-diff correctness.\n\n- **Run stuck in RUNNING on unhandled error** (`core.py`):\n  If `save_state`, `_resolve_next_step`, or JSON serialization threw after the step-level try/except, the exception escaped without updating run status. The run was permanently stuck as RUNNING and unrecoverable (resume rejects RUNNING runs). Split `_execute_steps_async` into a wrapper that catches unexpected errors and marks the run FAILED before re-raising.\n\n- **Replay crashes on any run with a failed step** (`replay.py`):\n  The replay loop unconditionally required `state_after is not None`, but failed steps have `state_after = None` by design. Any run containing a failed step \u2014 including `COMPLETED_WITH_ERRORS` \u2014 could not be replayed. Changed to only require `state_before`; when `state_after` is None, state carries forward unchanged.\n\n- **`RunState.data` returns `MappingProxyType` when frozen** (`core.py`):\n  After `run.freeze()`, `run.state.data` returned a `MappingProxyType`, not a `dict`. `json.dumps()` raised `TypeError` on the result. Changed to return `dict(current)` \u2014 a plain copy that's JSON-serializable. Removed unused `MappingProxyType` import.\n\n### P1 \u2014 Branch Evaluation\n\n- **`_DotDict` missing `__len__` and `__bool__`** (`utils.py`):\n  Branch conditions using `len(state.inputs)` raised `TypeError` because `_DotDict` had no `__len__`. Empty dicts wrapped in `_DotDict` evaluated as truthy (Python default) causing wrong branch selection. Added both methods delegating to the wrapped data.\n\n- **`_SafeExprValidator` rejects `not` operator** (`utils.py`):\n  `ast.UnaryOp` was allowed but operator nodes (`ast.Not`, `ast.USub`, etc.) were not. Branch conditions like `not state.inputs.is_low` failed with `ValueError(\"Unsupported expression\")`. Added `ast.Not`, `ast.USub`, `ast.UAdd`, `ast.Invert`, and arithmetic operators to the allowlist.\n\n### P2 \u2014 Security Hardening\n\n- **`_SafeExprValidator` blocks dunder attribute access** (`utils.py`):\n  Added `visit_Attribute` method that rejects attribute names starting with `_`. Prevents expressions like `state.__init__.__globals__` from leaking the module's namespace through branch conditions.\n\n## Security TODOs Added\n\n- **FileTool `_safe_path` prefix bypass** (`tools/file.py`):\n  `startswith(self.root)` is defeated by sibling directories sharing a prefix (e.g., `/project` vs `/projectx`). TODO documents the fix: `resolved == self.root or resolved.startswith(self.root + os.sep)`.\n\n- **`import_agent` symlink + manifest path traversal** (`agent/packaging.py`):\n  Two vulnerabilities: (1) tar symlink members bypass path validation, (2) manifest paths (`workflow`, `handlers`, `tools`) are joined to project_root without traversal checks. TODO documents both fixes.\n\n## Product TODOs Added\n\nAcross 14 source files, tagged with categories:\n\n| Tag | Files | What |\n|---|---|---|\n| `packaging` | `requirements.txt`, `README.md`, `config.py` | `pyproject.toml`, PyPI publishing, Makefile, single-binary distribution |\n| `sdk` | `__init__.py` | Programmatic API: `run_workflow()`, async-first embedding |\n| `ux` | `cli.py`, `config.py`, `README.md` | Solo dev ICP: `ai quickstart`, progress output, scaffolded LLM agent, input type coercion |\n| `streaming` | `core.py` | `on_event` callback for real-time step/token progress |\n| `testing` | `llm/adapters.py`, `llm/handler.py` | Missing OpenAI adapter test, LLM handler E2E test |\n| `example` | `llm/handler.py` | Real-world `handler: llm` workflow sample |\n| `roadmap` | `storage/base.py`, `logging.py`, `core.py`, memory tiers | Postgres backend, OpenTelemetry, Prometheus, multi-agent composition |\n\n## Tests Added\n\n- **`tests/test_builtin_tools.py`** \u2014 `TestValidateInput` (8 tests): missing required field, multiple required fields, present passes, None value passes, no required array, empty schema, HttpTool schema enforcement, FileTool schema enforcement.\n\n- **`tests/test_storage_roundtrip.py`** \u2014 `TestAppendStepEmptyDict` (6 tests): empty input/output/state_before/state_after roundtrip, None stays None, populated dict roundtrip.\n\n## Features\n\n- **Gemini provider support** (`llm/adapters.py`, `llm/client.py`, `llm/__init__.py`):\n  Added a Gemini adapter, request/response normalization, and client wiring.\n  Sample workflow: `workflows/samples/06_gemini_call.yaml`.\n\n- **Onboarding wizard** (`cli.py`, `docs/guide/onboarding-walkthrough.md`, `scripts/onboard.sh`):\n  Added `ai` (no args) and `ai onboard` guided setup, plus a reusable onboarding script.\n\n- **Setup enhancements** (`cli.py`):\n  Added `ai setup --check` for provider/key verification and `.env` auto-loading.\n", "changelog/CHANGELOG_2026-03-18.md": "# Changelog \u2014 2026-03-18\n\n## Features\n\n- **End-to-end timing telemetry**:\n  Per-step duration is now computed before event emission, and model/tool call\n  latencies are captured (`handler_duration_ms`, `tool_duration_ms`) for each step.\n  Run-level total duration is derived from run start/completion timestamps.\n\n- **Visualization enhancements**:\n  HTML/ASCII outputs now show run summary (start, completion, total duration)\n  and per-step call durations. HTML tool tables include tool latency.\n\n- **CLI progress output**:\n  `ai run` step progress now prints `n/a` instead of `None` and includes call duration\n  when available.\n\n## UX\n\n- **`ai visualize --html` auto-opens**:\n  HTML visualization now opens the browser by default. Use `--no-open` to disable.\n\n## Housekeeping\n\n- **Ignore generated run artifacts**:\n  Added `.runs/` to `.gitignore`.\n\n## Documentation\n\n- **Full documentation refresh** (all files updated to reflect 2026-03-18 codebase state):\n  - `.copilot/instructions.md` \u2014 added pyproject.toml, security-sensitive areas (shell allowlist, symlink rejection), memory namespacing, lifecycle hooks, overwrite policy notes\n  - `.copilot/project_context.md` \u2014 updated subsystem status table (17 \u2192 18 entries), corrected packaging status, memory tier status (working + semantic now \"Implemented\")\n  - `.copilot/session_state.md` \u2014 full rewrite for 2026-03-18 session with prior session summary\n  - `.copilot/working_set.md` \u2014 updated focus area, file list, core files, test inventory (21 tests)\n  - `.copilot/resume_prompt.md` \u2014 updated with 2026-03-18 state, 14 CLI commands, timing telemetry\n  - `.copilot/architecture_decisions.md` \u2014 added ADR-011 (timing telemetry)\n  - `.github/prompts/start-session.prompt.md` \u2014 updated doc references, added architecture rules\n  - `docs/about/architecture.md` \u2014 updated memory subsystem (\u00a78: working, semantic, episodic detail), config fields (\u00a716: overwrite policy, memory limits, shell, LLM), added lifecycle hooks (\u00a717) and timing telemetry (\u00a718) sections, renumbered extensions/LLM to \u00a719/\u00a720\n  - `docs/guide/onboarding-walkthrough.md` \u2014 fixed hardcoded absolute macOS path to relative `scripts/onboard.sh`\n  - `docs/about/gaps_2026-03-17.md` \u2014 marked gaps 3 (memory), 5 (input coercion), 6 (security) as RESOLVED; gap 2 (streaming) as partially resolved\n  - `docs/about/status_2026-03-17.md` \u2014 updated memory subsystem table, added 2026-03-18 visualization note\n  - `docs/changelog/CHANGELOG_2026-03-18.md` \u2014 this file (expanded with documentation section)\n", "changelog/CHANGELOG_2026-03-19.md": "# Changelog \u2014 2026-03-19\n\n## Documentation\n\n- **Docs reorganization**:\n  Added `docs/guide/`, `docs/about/`, and `docs/changelog/` folders to make onboarding,\n  reference material, and change history easier to find.\n\n- **New guides**:\n  Added `docs/guide/manual.md`, `docs/guide/workflows.md`, `docs/guide/handlers.md`,\n  and `docs/guide/tools.md` plus a guide index in `docs/guide/README.md`.\n\n- **Docs UI**:\n  Added `docs/site/index.html` with a lightweight navigator, renderer, and search.\n\n- **Moved references**:\n  Updated README and changelog references to point at the new doc locations.\n\n## Housekeeping\n\n- **Changelog index**:\n  Added `docs/changelog/README.md` and indexed existing entries under it.\n", "changelog/README.md": "**Changelog**\n\nThis folder collects dated change notes for the codebase and documentation.\n\n**Format**\n\nEach entry should include:\n- date\n- summary\n- key changes grouped by area (runtime, CLI, docs, tests)\n- migration notes if behavior changed\n\n**Entries**\n\n- `docs/changelog/CHANGELOG_2026-03-19.md`\n- `docs/changelog/CHANGELOG_2026-03-18.md`\n- `docs/changelog/CHANGELOG_2026-03-17.md`\n- `docs/changelog/CHANGELOG_2026-03-16.md`\n", "guide/README.md": "**Guide**\n\nUse these documents to get started quickly and operate the runtime day to day.\n\n- `docs/guide/getting-started.md`\n- `docs/guide/manual.md`\n- `docs/guide/usage.md`\n- `docs/guide/workflows.md`\n- `docs/guide/handlers.md`\n- `docs/guide/tools.md`\n- `docs/guide/onboarding-walkthrough.md`\n- `docs/guide/execution-walkthrough.md`\n", "guide/execution-walkthrough.md": "<!--\nFile: docs/guide/execution-walkthrough.md\nPurpose: Step-by-step execution narrative for run, resume, replay, and visualization flows.\nDescription: Shows runtime bootstrapping, state transitions, persistence checkpoints, and debug loops.\nDependencies: CLI and runtime internals under src/agent_runtime.\nInputs/Outputs: Input for developer learning; output is operational mental model.\nSide Effects: None.\n-->\n\n# Execution Walkthrough\n\nThis walkthrough is a textbook-style trace of what happens at runtime for a typical workflow run, then failure/resume, then replay.\n\nPrimary command used:\n\n```bash\nai run workflows/example.yaml -i issue=\"Login API fails for invalid token\"\nai run example_workflow -i issue=\"Login API fails for invalid token\"\nai run code_review_agent@v2 -i issue=\"Login API fails for invalid token\"\n```\n\n## 1. Runtime bootstrapping\n\n1. CLI parses arguments (`workflow_ref`, `db-path`, `-i` key=value inputs).\n2. Handler registry is created and model handlers are registered.\n3. Tool registry is created and default tools (for example `tools.echo`) are registered.\n4. Workflow is resolved either by file path, `workflow_id`, or `workflow_id@version`, then validated.\n\nValidation includes:\n- step ids unique\n- step type correctness\n- branching rule structure\n- `goto` targets exist\n- retry policy values valid\n- step contract checks (future-read and output-collision prevention)\n\n## 2. Run creation\n\nExecutor creates a run record:\n- `status = PENDING`\n- `workflow_id` and `workflow_version` captured\n- `workflow_hash` and `input_hash` captured\n- initial structured state:\n\n```json\n{\n  \"inputs\": {\n    \"issue\": \"Login API fails for invalid token\"\n  },\n  \"steps\": {},\n  \"runtime\": {}\n}\n```\n\nThen run transitions to `RUNNING` and initial state is persisted as `state_versions.version = 0`.\n\n### Memory hydration\n\nBefore step execution begins, the runtime calls `MemoryManager.hydrate_state(state)`, which reads from all four memory tiers (working, episodic, semantic, procedural) and enriches the initial state.\n\n## 3. Step 1 execution (`generate_summary`)\n\nStep definition:\n- type: `model`\n- inputs mapping: `issue: inputs.issue`\n\nDetailed flow:\n1. Runtime captures `state_before` snapshot.\n2. Step input is materialized from state path mapping.\n3. Handler receives a `RuntimeState` wrapper of step input.\n4. Handler returns structured output, for example:\n\n```json\n{\"summary\": \"Issue related to login api failing when invalid token.\"}\n```\n\n5. Runtime writes output to `steps.generate_summary` namespace.\n6. If `outputs` contract is declared, runtime validates output keys against the contract.\n7. Runtime captures `state_after` snapshot.\n8. Persists `StepExecution` and increments state version.\n\n## 4. Step 2 execution (`echo_tool`)\n\nStep definition:\n- type: `tool`\n- inputs mapping: `message: steps.generate_summary.summary`\n\nDetailed flow:\n1. Runtime captures `state_before`.\n2. Tool input is resolved from state paths.\n3. Tool input is validated against tool schema.\n4. Tool receives `RuntimeContext` (`run_id`, `step_id`, state, logger).\n5. Runtime emits tool events (`TOOL_START`, `TOOL_SUCCESS` or `TOOL_ERROR`).\n6. Tool output is written to `steps.echo_tool`.\n7. Runtime captures `state_after` and persists step/state records.\n\n## 5. Run completion\n\nAfter final step:\n- `status -> COMPLETED` (or `COMPLETED_WITH_ERRORS` in continue mode)\n- `MemoryManager.persist_state(state)` writes final state to all memory tiers\n- terminal timestamps persisted\n- run state frozen from runtime perspective\n\n## 6. What inspect shows\n\nSummary view:\n\n```bash\nai inspect <run_id>\n```\n\nShows:\n- run metadata\n- ordered step statuses\n- duration and attempt counts\n- latest state\n\nStep-centric view:\n\n```bash\nai inspect <run_id> --steps\n```\n\nShows per step:\n- status\n- attempts\n- output\n- error / last_error\n\nState timeline view:\n\n```bash\nai inspect <run_id> --state-history\n```\n\nShows:\n- initial state\n- per-step diffs (`+`, `-`, `~`)\n- output and state-after snapshots\n\n## 7. Failure and resume walkthrough\n\nTrigger a failing workflow:\n\n```bash\nai run workflows/samples/04_fail_and_resume.yaml -i issue=\"Login API fails\"\n```\n\nExpected behavior:\n- earlier steps may be `COMPLETED`\n- failing step is `FAILED`\n- run is `FAILED`\n\nResume:\n\n```bash\nai resume <run_id>\n```\n\nResume flow:\n1. Validate run is resumable.\n2. Validate workflow hash.\n3. Determine resume step from step history.\n4. Load latest state snapshot.\n5. Continue execution from resume step with normal retry/branch logic.\n\n## 8. Replay walkthrough (deterministic simulation)\n\nReplay command:\n\n```bash\nai replay <run_id>\n```\n\nReplay flow:\n1. Load run record.\n2. Load ordered step history.\n3. Load initial state.\n4. Iterate steps in recorded order.\n5. Inject recorded transitions (`state_before` -> `state_after`).\n\nImportant:\n- replay does not call model handlers\n- replay does not call tools\n- replay does not mutate persisted run data\n\nVerification mode:\n\n```bash\nai replay <run_id> --verify-state\n```\n\nThis checks reconstructed state matches recorded `state_before` at each step.\n\n## 9. Data persisted per step (for debugging)\n\nEach step execution persists:\n- `execution_index`\n- `status`\n- `attempt_count`\n- `input`\n- `output`\n- `error` and `last_error`\n- `state_before`\n- `state_after`\n- timing (`started_at`, `finished_at`, `duration_ms`)\n\nThis is the foundation for deterministic debugging and reproducibility.\n\n## 10. Practical debugging playbook\n\n1. Run workflow.\n2. Inspect with `--steps` to identify failing step and attempt pattern.\n3. Inspect with `--state-history` to verify exact mutation point.\n4. Resume if failure is recoverable.\n5. Replay with `--verify-state` for deterministic postmortem.\n\nThis cycle is the intended developer loop of the runtime.\n\n## 11. Visualization walkthrough\n\nGenerate a graph/timeline report for the same run:\n\n```bash\nai visualize <run_id>\n```\n\nThis creates:\n- `.runs/<run_id>/visualization.html`\n\nAnd includes:\n- execution graph (nodes, statuses, durations, attempts)\n- branch rule evaluations and selected path\n- step timeline with timing/error details\n- tool call table (arguments/results/latency)\n- state timeline with `+`, `-`, `~` key-path diffs\n\nFor terminal-only debugging:\n\n```bash\nai visualize <run_id> --ascii\nai visualize <run_id> --timeline\n```\n", "guide/getting-started.md": "**Getting Started**\n\nWelcome! This guide is intentionally beginner-friendly. It will get you from zero to a successful run, then show the smallest possible change you can make to feel real progress.\n\n**Install**\n\n```bash\npython -m venv .venv\nsource .venv/bin/activate\npip install -r requirements.txt\npip install -e .\n```\n\n**Quickstart (0 \u2192 1)**\n\n```bash\nmkdir my-agent\ncd my-agent\nai quickstart\n```\n\n`ai quickstart` does three things:\n1. Initializes a project scaffold (`workflows/`, `handlers/`, `tools/`, `agents/`, `runtime.yaml`).\n2. Runs the setup flow to configure an LLM provider and optional API key.\n3. Executes `workflows/example.yaml` so you see a successful run immediately.\n\n**Run Again With Different Input**\n\n```bash\nai run workflows/example.yaml -i issue=\"Login API fails for invalid token\"\n```\n\n**Inspect And Visualize**\n\n```bash\nai inspect <run_id> --steps\nai visualize <run_id> --html\n```\n\n**Make Your First Change (1 \u2192 2)**\n\n```bash\ncp workflows/example.yaml workflows/my_workflow.yaml\n```\n\nEdit `workflows/my_workflow.yaml` to swap a step handler or add a new step, then run it:\n\n```bash\nai run workflows/my_workflow.yaml\n```\n\nIf you add a new handler, put it in `handlers/` and reference it by name in the workflow. The runtime auto-discovers handler functions and tool classes from those folders.\n\nIf you want the full walkthrough, read `docs/guide/manual.md` next.\n\nIf you prefer a visual navigator, open `docs/site/index.html` in a browser.\n", "guide/handlers.md": "**Handlers**\n\nHandlers are the functions that power `model` steps. You can think of a handler as a small unit of logic that reads from the workflow state and returns a dictionary of new fields to merge back into state.\n\n**Where Handlers Live**\n\nPut your handler functions in the `handlers/` folder. The runtime auto-discovers them at run time.\n\n**Handler Shape**\n\n```python\nfrom agent_runtime.state import RuntimeState\n\ndef summarize_issue(state: RuntimeState) -> dict:\n    issue = state.get(\"issue\", \"\")\n    return {\"summary\": issue[:140]}\n```\n\n**How The Workflow Uses It**\n\n```yaml\nsteps:\n  - id: summarize\n    type: model\n    handler: summarize_issue\n    inputs:\n      issue: inputs.issue\n```\n\n**Return Values**\n\n- Return a plain dictionary.\n- Keys become new fields in `steps.<step_id>`.\n- You can safely return nested objects; they are stored in JSON.\n\n**Two Discovery Conventions**\n\n1. Zero-config: every public function (not starting with `_`) is registered by name.\n2. Explicit: define a `__handlers__` dict to control exported names.\n\nExample explicit registry:\n\n```python\ndef _internal_helper():\n    return \"ignored\"\n\ndef run_analysis(state):\n    return {\"result\": \"ok\"}\n\n__handlers__ = {\n    \"analyze\": run_analysis,\n}\n```\n\n**Common Tips**\n\n- Keep handlers pure and deterministic when possible.\n- Use `state.get(\"key\")` to read inputs and previous outputs.\n- Keep outputs small and structured so they are easy to inspect later.\n", "guide/manual.md": "**Agentic Runtime Manual**\n\nWelcome. This manual is written for first-time users and for people who want a complete reference in one place. If you only read one document, read this one.\n\n**Mental Model**\n\nA workflow is a recipe. Each step reads the current state, does some work, then writes new fields back to the state. The runtime records every step, every state change, and every error, so runs can be inspected, resumed, or replayed later.\n\n**Core Concepts**\n\n- Workflow: a YAML file that describes inputs and steps.\n- Step: a unit of work in the workflow.\n- State: the structured data passed between steps.\n- Handler: a Python function used by `model` steps.\n- Tool: a Python class used by `tool` steps.\n- Agent manifest: a portable bundle that references a workflow plus its handlers and tools.\n- Run: a persisted execution record stored in SQLite.\n\n**Quickstart**\n\n```bash\nmkdir my-agent\ncd my-agent\nai quickstart\n```\n\nThis creates the project structure, configures your LLM provider, and runs the starter workflow.\n\n**Project Structure**\n\n```\nmy-agent/\n  agents/\n  handlers/\n  tools/\n  workflows/\n  runtime.yaml\n  .env\n```\n\n**Writing A Workflow**\n\nMinimum shape:\n\n```yaml\nworkflow:\n  id: my_workflow\n  version: v1\ninputs:\n  issue:\n    description: The issue text\n    required: true\nsteps:\n  - id: summarize\n    type: model\n    handler: generate_summary\n    inputs:\n      issue: inputs.issue\n```\n\nReferencing data:\n\n- `inputs.<name>` reads workflow inputs.\n- `steps.<step_id>.<field>` reads prior step output.\n\nBranching:\n\n```yaml\nsteps:\n  - id: triage\n    type: model\n    handler: classify_severity\n    inputs:\n      issue: inputs.issue\n    branch:\n      when:\n        - if: steps.triage.severity == \"high\"\n          goto: escalate\n        - if: steps.triage.severity == \"low\"\n          goto: close\n```\n\nRetry policy:\n\n```yaml\nsteps:\n  - id: unstable_call\n    type: tool\n    tool: tools.http\n    inputs:\n      url: inputs.url\n    retry:\n      attempts: 3\n      backoff: exponential\n      initial_delay: 1\n```\n\nError handling:\n\n- `on_error: fail_fast` stops on the first error.\n- `on_error: continue` attempts the remaining steps.\n\nVersioning:\n\n```bash\nai run my_workflow\nai run my_workflow@v2\n```\n\n**Handlers (Model Steps)**\n\nHandler functions live in `handlers/` and are auto-discovered.\n\n```python\nfrom agent_runtime.state import RuntimeState\n\ndef summarize_issue(state: RuntimeState) -> dict:\n    issue = state.get(\"issue\", \"\")\n    return {\"summary\": issue[:140]}\n```\n\nWorkflow usage:\n\n```yaml\nsteps:\n  - id: summarize\n    type: model\n    handler: summarize_issue\n    inputs:\n      issue: inputs.issue\n```\n\n**Tools (Tool Steps)**\n\nTool classes live in `tools/` and are auto-discovered.\n\n```python\nfrom __future__ import annotations\n\nfrom typing import Any, Dict, Optional\nfrom agent_runtime.tools.base import RuntimeContext, ToolResult\n\nclass ExampleTool:\n    name = \"tools.example\"\n    description = \"Uppercases the provided text\"\n    input_schema = {\n        \"type\": \"object\",\n        \"properties\": {\"text\": {\"type\": \"string\"}},\n        \"required\": [\"text\"],\n    }\n    timeout: Optional[float] = None\n    retries: Optional[int] = None\n\n    async def execute(\n        self, input: Dict[str, Any], context: RuntimeContext\n    ) -> ToolResult:\n        text = input.get(\"text\", \"\")\n        return ToolResult(success=True, output={\"text\": text.upper()}, error=None, metadata=None)\n```\n\nWorkflow usage:\n\n```yaml\nsteps:\n  - id: shout\n    type: tool\n    tool: tools.example\n    inputs:\n      text: inputs.message\n```\n\n**Running Workflows**\n\n```bash\nai run workflows/example.yaml\nai run workflows/example.yaml -i issue=\"Login API fails for invalid token\"\n```\n\n**Inspecting Runs**\n\n```bash\nai inspect <run_id> --steps\nai visualize <run_id> --html\n```\n\n**Resuming And Replaying**\n\n```bash\nai resume <run_id>\nai replay <run_id> --verify-state\n```\n\n**Common Errors**\n\n- Unknown handler: confirm the function name is public and in `handlers/`.\n- Unknown tool: confirm the tool class has a `name` and is in `tools/`.\n- Missing inputs: add `-i key=value` or set defaults in the workflow.\n- YAML errors: run `ai validate <agent.yaml>` or fix indentation issues.\n\n**Security And Safety**\n\n- Keep API keys in `.env` or environment variables, not in code.\n- Use shell allowlists in `runtime.yaml` if you enable the shell tool.\n\nIf you want deeper reference material, see `docs/guide/usage.md` and `docs/guide/workflows.md`.\n", "guide/onboarding-walkthrough.md": "# Onboarding Walkthrough\n\nThis walkthrough is a guided path for a first-time user, designed to match\nthe \"single-command onboarding\" style common in modern CLIs. It pairs a\nquickstart wizard with optional manual steps for folks who prefer scripts.\n\n## Quickstart (Interactive)\n\nIf you do not have a project folder yet, create one first:\n\n```bash\nmkdir my-agent\ncd my-agent\n```\n\nRun either of the following in your project root:\n\n```bash\nai\n```\n\nor:\n\n```bash\nai onboard\n```\n\nThe home screen will offer numbered actions; choose **Guided setup** to launch the wizard.\n\nThe wizard will:\n1. Initialize the project structure if missing.\n2. Configure an LLM provider and API key.\n3. Offer to run a sample workflow.\n\n## Visual Walkthrough\n\n```mermaid\nflowchart TD\n  A[\"Start: User runs `ai` or `ai onboard`\"] --> B{\"runtime.yaml exists?\"}\n  B -- No --> C[\"Initialize project structure\"]\n  B -- Yes --> D[\"Load .env and continue\"]\n  C --> D\n  D --> E[\"Choose provider + enter API key\"]\n  E --> F[\"Write .env + update runtime.yaml\"]\n  F --> G{\"Run sample now?\"}\n  G -- Yes --> H[\"Run provider-specific sample workflow\"]\n  G -- No --> I[\"Show next steps + commands\"]\n  H --> J[\"Inspect + visualize run\"]\n  I --> J\n```\n\n## Scripted Walkthrough\n\nRemoved \u2014 use `ai onboard` for the guided flow.\n", "guide/tools.md": "**Tools**\n\nTools are the classes that power `tool` steps. A tool wraps an external action, like calling an API, reading a file, or running a shell command.\n\n**Where Tools Live**\n\nPut tool classes in the `tools/` folder. The runtime auto-discovers them at run time.\n\n**Tool Shape**\n\n```python\nfrom __future__ import annotations\n\nfrom typing import Any, Dict, Optional\nfrom agent_runtime.tools.base import RuntimeContext, ToolResult\n\nclass ExampleTool:\n    name = \"tools.example\"\n    description = \"Uppercases the provided text\"\n    input_schema = {\n        \"type\": \"object\",\n        \"properties\": {\n            \"text\": {\"type\": \"string\"},\n        },\n        \"required\": [\"text\"],\n    }\n    timeout: Optional[float] = None\n    retries: Optional[int] = None\n\n    async def execute(\n        self, input: Dict[str, Any], context: RuntimeContext\n    ) -> ToolResult:\n        text = input.get(\"text\", \"\")\n        return ToolResult(\n            success=True,\n            output={\"text\": text.upper()},\n            error=None,\n            metadata=None,\n        )\n```\n\n**How The Workflow Uses It**\n\n```yaml\nsteps:\n  - id: shout\n    type: tool\n    tool: tools.example\n    inputs:\n      text: inputs.message\n```\n\n**Input Validation**\n\n- `input_schema` uses JSON Schema.\n- Invalid inputs fail before the tool runs.\n- Use this to keep failures clear and early.\n\n**Timeouts And Retries**\n\n- Set `timeout` for slow external calls.\n- Set `retries` for transient failures.\n- You can also use step-level retry policy in the workflow YAML.\n\n**Tool Context**\n\n`RuntimeContext` includes references to the storage layer and current run metadata. Use it for advanced integrations that need run ids or storage access.\n", "guide/usage.md": "<!--\nFile: docs/guide/usage.md\nPurpose: Command-focused usage reference for daily runtime operations.\nDescription: Covers setup, running workflows, inspect/resume/replay flows, and common debugging patterns.\nDependencies: `ai` CLI and sample workflows.\nInputs/Outputs: Input for users; output is executable command guidance.\nSide Effects: None.\n-->\n\n# Usage\n\nThis guide is command-first and scenario-oriented. If you are brand new, start with `docs/guide/getting-started.md` and then return here.\n\n## 1. Prerequisites\n\nUse the shared conda environment and install the CLI in editable mode so `ai` is on your PATH:\n\n```bash\nconda activate agent_runtime\npip install -r requirements.txt\npip install -e .\n```\n\n## 2. Create a new agent project\n\nPick a folder for your agent. The runtime uses the current directory as the project root.\n\n```bash\nmkdir my-agent\ncd my-agent\nai quickstart\n```\n\n`ai quickstart` initializes the project (if needed), configures an LLM provider,\nwrites `.env`, and runs a starter workflow.\n\nPrefer non-interactive setup?\n\n```bash\nai init\n```\n\nRun the wizard from another directory:\n\n```bash\nai onboard --path my-project\n```\n\nWhen run with no args, `ai` opens a home screen with common actions\n(setup, run sample, inspect, visualize).\n\nCreates a project structure:\n\n```\n\u251c\u2500\u2500 workflows/\n\u2502   \u2514\u2500\u2500 example.yaml           # example workflow definition\n\u251c\u2500\u2500 handlers/\n\u2502   \u2514\u2500\u2500 example_handler.py     # example model step handler\n\u251c\u2500\u2500 tools/\n\u2502   \u2514\u2500\u2500 example_tool.py        # example tool implementation\n\u2514\u2500\u2500 runtime.yaml               # runtime configuration\n```\n\n- `workflows/` \u2014 YAML workflow definitions\n- `handlers/` \u2014 Python functions for `model` step handlers\n- `tools/` \u2014 Python classes implementing the `Tool` protocol for `tool` steps\n- `runtime.yaml` \u2014 runtime configuration (db path, directory paths, model backend settings)\n\n## 3. Run workflows\n\nWorkflow files should declare:\n\n```yaml\nworkflow:\n  id: my_workflow\n  version: v1\ninputs:\n  issue:\n    description: The issue text to process\n    required: true\n  priority:\n    description: Optional priority level\n    required: false\n    default: \"medium\"\n```\n\nThe `inputs:` block declares what the workflow expects from the caller.\nInputs can specify `description`, `required` (default `true`), and `default`.\nA shorthand list form is also supported: `inputs: [issue, priority]` (all required, no defaults).\n\nWorkflows without an `inputs:` block still work \u2014 the runtime infers\navailable inputs from step references (backward compatible).\n\n## Run default example\n\n```bash\nai run workflows/example.yaml\n```\n\n## Run latest workflow version by id\n\n```bash\nai run example_workflow\n```\n\nResolution rule:\n- runtime scans `./workflows/**/*.yaml` and `./workflows/**/*.yml`\n- selects highest numeric `vN` for matching `workflow.id`\n\n## Run a specific workflow version\n\n```bash\nai run code_review_agent@v2\n```\n\n## Run with custom inputs\n\nPass inputs with the `-i` / `--input` flag (repeatable):\n\n```bash\nai run workflows/example.yaml -i issue=\"Login API fails for invalid token\"\nai run code_review_agent@v1 -i issue=\"Login API fails for invalid token\"\n```\n\nMultiple inputs:\n\n```bash\nai run my_workflow.yaml -i issue=\"bug report\" -i priority=\"high\"\n```\n\nIf the workflow declares defaults, you can omit inputs that have them.\n\n## Use a custom SQLite path\n\n```bash\nai run workflows/example.yaml --db-path runtime.db\n```\n\n## 4. Inspect runs\n\n## Summary mode\n\n```bash\nai inspect <run_id>\n```\n\nUse when you want:\n- run status\n- ordered step outcome\n- latest state snapshot\n\n## Step-centric mode\n\n```bash\nai inspect <run_id> --steps\n```\n\nUse when you want:\n- per-step output\n- attempt counts\n- exact error details\n\n## State evolution mode\n\n```bash\nai inspect <run_id> --state-history\n```\n\nUse when you want:\n- initial state\n- per-step mutation history\n- state diffs and post-step snapshots\n\n## 5. Resume failed runs\n\n```bash\nai resume <run_id>\n```\n\nUse when:\n- a run failed mid-flow\n- completed steps should not re-run\n\nBehavior:\n- validates run status and workflow compatibility\n- restores latest state\n- continues from resume step\n\n## 6. Deterministic replay\n\n## Replay full run\n\n```bash\nai replay <run_id>\n```\n\n## Replay with consistency verification\n\n```bash\nai replay <run_id> --verify-state\n```\n\n## Replay until a specific step\n\n```bash\nai replay <run_id> --until summarize\n```\n\n## Replay interactively (step-by-step)\n\n```bash\nai replay <run_id> --step-by-step\n```\n\nReplay guarantees:\n- no tool invocation\n- no model invocation\n- no persisted data mutation\n\n## 7. Sample workflows\n\nRun all curated samples:\n\n```bash\nai run workflows/samples/01_linear_issue_summary.yaml\nai run workflows/samples/02_retry_and_backoff.yaml\nai run workflows/samples/03_branching_triage.yaml -i issue=\"bug\"\nai run workflows/samples/04_fail_and_resume.yaml -i issue=\"Login API fails\"\nai run workflows/samples/versioning/code_review_agent_v1.yaml\nai run workflows/samples/versioning/code_review_agent_v2.yaml\n```\n\nWhat each sample demonstrates:\n- `01_linear_issue_summary.yaml`: baseline linear execution\n- `02_retry_and_backoff.yaml`: retry semantics and attempt visibility\n- `03_branching_triage.yaml`: deterministic conditional branching\n- `04_fail_and_resume.yaml`: failure path and resume flow\n- `versioning/code_review_agent_v1.yaml` + `versioning/code_review_agent_v2.yaml`: workflow version evolution\n\n## 8. Step contracts workflow pattern\n\n### Writing a handler\n\nA handler is a Python function that does the actual work for a `model` step. The runtime calls it, passing in the step's input state, and expects a dict back.\n\nSignature:\n```python\ndef my_handler(state: RuntimeState) -> Dict[str, Any]:\n    # read inputs from state\n    # do work (call an LLM, transform data, etc.)\n    # return a dict of outputs\n```\n\nExample \u2014 a handler that classifies issue severity:\n```python\ndef classify_severity(state: RuntimeState) -> Dict[str, Any]:\n    issue = state[\"issue\"]\n    if \"crash\" in issue.lower() or \"down\" in issue.lower():\n        return {\"severity\": \"critical\", \"reason\": \"Service impact detected\"}\n    return {\"severity\": \"low\", \"reason\": \"No immediate impact\"}\n```\n\n### Handler auto-discovery\n\nThe runtime automatically discovers handlers from the `handlers/` directory. Just drop a `.py` file there and your handlers are available.\n\n**Convention 1 \u2014 zero-config:** every public function (not starting with `_`) is registered using the function name.\n\n```\nhandlers/\n\u2514\u2500\u2500 my_handlers.py     # contains classify_severity() \u2192 available as handler: classify_severity\n```\n\n**Convention 2 \u2014 explicit:** define a `__handlers__` dict for full naming control.\n\n```python\n# handlers/my_handlers.py\n\ndef _internal_helper():\n    pass\n\ndef _my_classify(state):\n    return {\"severity\": \"low\", \"reason\": \"No impact\"}\n\n__handlers__ = {\n    \"classify_severity\": _my_classify,\n}\n```\n\nBuilt-in handlers (`generate_summary`, `classify_severity`, `diagnose_issue`, `propose_fix`, `review_code`) are always available regardless of what's in `handlers/`.\n\nUsing a handler in a workflow:\n```yaml\nsteps:\n  - id: triage\n    type: model\n    handler: classify_severity\n    inputs:\n      issue: inputs.issue\n```\n\nWhen this step runs, the runtime:\n1. Builds the input state from the `inputs` mapping\n2. Calls `classify_severity(input_state)`\n3. Writes the returned dict to `steps.triage` in state\n4. Persists `state_before`, `state_after`, timing, and attempt count to SQLite\n\nHandlers vs tools:\n- **Handlers** = plain Python functions for `model` steps (type: `model`, field: `handler`)\n- **Tools** = objects implementing the `Tool` protocol for `tool` steps (type: `tool`, field: `tool`)\n\n### Writing a tool\n\nTools are classes that implement the `Tool` protocol. Drop a `.py` file in `tools/` and the runtime discovers it automatically.\n\nTool protocol:\n```python\nclass MyTool:\n    name = \"tools.my_tool\"             # unique tool name\n    description = \"What this tool does\"\n    input_schema = {                    # JSON Schema for input validation\n        \"type\": \"object\",\n        \"properties\": {\n            \"message\": {\"type\": \"string\"},\n        },\n    }\n    timeout: Optional[float] = None     # optional execution timeout\n    retries: Optional[int] = None       # optional retry count\n\n    async def execute(self, input: Dict[str, Any], context: RuntimeContext) -> ToolResult:\n        # Do work here (API call, file operation, etc.)\n        return ToolResult(success=True, output={\"result\": \"...\"},\n                          error=None, metadata=None)\n```\n\nDiscovery convention: every class whose name doesn't start with `_` and satisfies the protocol is auto-registered. No manual registration needed.\n\nUsing a tool in a workflow:\n```yaml\nsteps:\n  - id: shout\n    type: tool\n    tool: tools.my_tool\n    inputs:\n      message: inputs.text\n```\n\nBuilt-in tools (`tools.echo`) are always available regardless of what's in `tools/`.\n\n### Using step contracts\n\n```yaml\nworkflow:\n  id: contracts_demo\n  version: v1\ninputs_contract: [issue]\nsteps:\n  - id: generate_summary\n    type: model\n    handler: generate_summary\n    inputs: [issue]\n    outputs: [summary]\n```\n\nBehavior:\n- Contract input keys are validated against available state symbols.\n- Contract output keys are checked for collisions across steps.\n- Runtime enforces that step output keys match declared `outputs`.\n\n## 9. Common workflows for developers\n\n## A) Debug a failure\n\n1. Run workflow.\n2. Inspect step details.\n3. Inspect state history.\n\n```bash\nai run workflows/samples/04_fail_and_resume.yaml -i issue=\"Login API fails\"\nai inspect <run_id> --steps\nai inspect <run_id> --state-history\n```\n\n## B) Recover after fixing an adapter/config\n\n```bash\nai resume <run_id>\n```\n\n## C) Reproduce exactly for postmortem\n\n```bash\nai replay <run_id> --verify-state --step-by-step\n```\n\n## 10. Test suite\n\n```bash\npytest -q\n```\n\nFor targeted checks:\n\n```bash\npytest -q tests/test_replay.py\npytest -q tests/test_state_manager.py\n```\n\n## 11. Runtime configuration\n\nThe runtime reads settings from `runtime.yaml` in the project root. CLI flags override config values.\n\nExample `runtime.yaml` (generated by `ai init`):\n\n```yaml\ndb_path: runtime.db\nworkflows_dir: workflows\nhandlers_dir: handlers\ntools_dir: tools\n\n# llm:\n#   providers:\n#     openai:\n#       api_key_env: OPENAI_API_KEY\n#       models:\n#         gpt-4o:\n#           temperature: 0.2\n#           max_tokens: 4096\n#     anthropic:\n#       api_key_env: ANTHROPIC_API_KEY\n#       models:\n#         claude-3-opus:\n#           temperature: 0.3\n#     gemini:\n#       api_key_env: GEMINI_API_KEY\n#       models:\n#         gemini-2.5-flash:\n#           temperature: 0.2\n#           max_tokens: 8192\n\n# logging:\n#   level: info\n#   format: json\n```\n\nTip: `ai setup` can scaffold this config and write `.env`, and\n`ai setup --check` validates which provider keys are available.\n\nPrecedence: `--db-path` flag > `runtime.yaml` value > built-in default (`runtime.db`).\n\nIf `runtime.yaml` does not exist, all built-in defaults apply.\n\n## 12. Troubleshooting\n\n## `Cannot replay RUNNING run`\n- Wait for run completion/failure, then replay.\n\n## `Workflow hash mismatch; cannot resume`\n- Workflow changed since original run.\n- Resume requires compatible workflow definition.\n\n## `Replay data missing`\n- Step/state persistence is incomplete for that run.\n- Re-run workflow with current runtime version.\n\n## `ModuleNotFoundError: yaml`\n- Install dependencies in the active environment:\n\n```bash\npip install -r requirements.txt\n```\n\n## 13. State diff debugging\n\nShow state changes across all recorded steps:\n\n```bash\nai state-diff <run_id>\n```\n\nShow state changes for one step id:\n\n```bash\nai state-diff <run_id> --step plan\n```\n\nOutput markers:\n- `+` added key path\n- `-` removed key path\n- `~` modified key path\n\n## 14. Run visualization\n\nASCII graph + timeline:\n\n```bash\nai visualize <run_id> --ascii\n```\n\nTimeline-focused text:\n\n```bash\nai visualize <run_id> --timeline\n```\n\nHTML visualization (default mode, auto-opens browser):\n\n```bash\nai visualize <run_id>\n```\n\nHTML visualization with explicit HTML mode (also auto-opens browser):\n\n```bash\nai visualize <run_id> --html\n```\n\nHTML visualization without auto-opening browser:\n\n```bash\nai visualize <run_id> --html --no-open\n```\n\nOutput:\n- file path `.runs/<run_id>/visualization.html`\n- default mode attempts to open the file in the default browser\n- `--html` also opens the browser unless `--no-open` is provided\n\nHTML includes:\n- execution graph view\n- branch decision table\n- run summary (start, completion, total duration)\n- per-step call durations (tool/LLM)\n- step timeline with attempts/duration\n- tool call table (args/result/latency)\n- state timeline with per-step diffs\n", "guide/workflows.md": "**Writing Workflows**\n\nIf you are new here, think of a workflow as a recipe. Each step reads from the current state, does a unit of work, then writes new fields back into the state for later steps to use.\n\nThis guide covers the workflow YAML format, the step types, and the patterns you will use most often.\n\n**Basic Shape**\n\n```yaml\nworkflow:\n  id: issue_triage\n  version: v1\ninputs:\n  issue:\n    description: The issue text to analyze\n    required: true\non_error: fail_fast\nsteps:\n  - id: summarize\n    type: model\n    handler: generate_summary\n    inputs:\n      issue: inputs.issue\n  - id: echo\n    type: tool\n    tool: tools.echo\n    inputs:\n      message: steps.summarize.summary\n```\n\n**Inputs**\n\n- `inputs` declares what the workflow expects from the caller.\n- Each input can define `description`, `required` (default `true`), and `default`.\n- If you omit `inputs`, the runtime will accept any inputs passed by the caller.\n\nExample call:\n\n```bash\nai run workflows/example.yaml -i issue=\"Login API fails for invalid token\"\n```\n\n**Step Types**\n\n`model` step:\n- Uses a handler function registered in `handlers/` or built in.\n- `handler` is the handler name, not a file path.\n\n`tool` step:\n- Uses a tool class registered in `tools/`.\n- `tool` is the tool name (for example `tools.echo`).\n\n**Referencing Data**\n\nUse `inputs.<name>` to read workflow inputs and `steps.<step_id>.<field>` to read prior outputs.\n\nExample:\n\n```yaml\ninputs:\n  issue:\n    required: true\nsteps:\n  - id: summarize\n    type: model\n    handler: generate_summary\n    inputs:\n      issue: inputs.issue\n  - id: classify\n    type: model\n    handler: classify_severity\n    inputs:\n      summary: steps.summarize.summary\n```\n\n**Retry Policy**\n\n```yaml\nsteps:\n  - id: unstable_call\n    type: tool\n    tool: tools.http\n    inputs:\n      url: inputs.url\n    retry:\n      attempts: 3\n      backoff: exponential\n      initial_delay: 1\n```\n\n**Branching**\n\n```yaml\nsteps:\n  - id: triage\n    type: model\n    handler: classify_severity\n    inputs:\n      issue: inputs.issue\n    branch:\n      when:\n        - if: steps.triage.severity == \"high\"\n          goto: escalate\n        - if: steps.triage.severity == \"low\"\n          goto: close\n```\n\nBranches must point to valid step ids. The runtime will validate this at load time.\n\n**Error Policy**\n\nWorkflow-level `on_error` can be:\n- `fail_fast` (default)\n- `continue` (attempts to complete remaining steps)\n\n**Versioning**\n\nUse `workflow.version` like `v1`, `v2`, etc. The CLI resolves the highest version when you run by id:\n\n```bash\nai run issue_triage\nai run issue_triage@v2\n```\n\n**Handlers And Tools**\n\n- Put handler functions in `handlers/`. Public functions are auto-registered.\n- Put tools in `tools/`. Public classes implementing the Tool protocol are auto-registered.\n\nIf you change handlers or tools, re-run the same workflow file and the runtime will discover the new code.\n\nFor deeper handler and tool coverage, see `docs/guide/handlers.md` and `docs/guide/tools.md`.\n"};
